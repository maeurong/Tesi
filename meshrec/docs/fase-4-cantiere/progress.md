# SDD ledger — plan: docs/superpowers/plans/2026-08-18-meshrec-fase-4-prior-telaio.md

Spec: `docs/superpowers/specs/2026-08-18-meshrec-fase-4-prior-telaio-design.md` (letta).
Worktree: `/Users/mario/GitHub/Tesi/.claude/worktrees/fase-4-materiale`, ramo `worktree-fase-4-materiale`.
Base di partenza: `f395ce8`. Suite di partenza: 402 passati, 3 saltati.

## Scan preliminare

### Coppie che condividono un file o un'interfaccia

| coppie | cosa produce l'una / consuma l'altra | esito |
|---|---|---|
| T1 → T2,3,4,7,9,10 | `WallConfig`, `ModelConfig`, `PipelineConfig.wall/.model`, `STEP_KEYS` a 12, `STEP_BLOCKS[12]`, `BLOCCHI_FUORI_IMPRONTA` | coerente. T1 è cancello per tutti |
| T2 ∩ T4 — file `src/meshrec/core/abaqus.py` | T2 rende pubblica `fix_sign`; T4 generalizza `boundary_faces` e `write_inp` | **stesso file.** L'architect li dava paralleli; io non dispaccio implementer in parallelo (regola della skill), quindi il rischio non si presenta. T2 prima, T4 dopo |
| T2 → T3 | `scomponi`, `terna` | coerente |
| T3 → T8, T9 | `Membratura`, `prior` | coerente |
| T4 → T5 | `NODI_PER_ELEMENTO`, `boundary_faces` | coerente |
| T4 → T6, T7 | `quality.hex_volumes` | **incoerente, vedi Ruling A** |
| T5 → T8, T10, T11 | `element_surface`, `surface_area`, `write_inp` esteso | coerente |
| T6 → T7, T10 | `scaled_jacobian`, `hexa_metrics` | coerente |
| T7 → T8 | `mesh_prisma`, `ordine_canonico`, `passo_di_mesh` | coerente |
| T8 → T10 | `costruisci` | coerente |
| T9 ∩ T10 ∩ T12 — file `src/meshrec/cli.py` | comandi `wall`, `model`, `compare` | coerente: tre comandi distinti, nessuno tocca gli altri. Sequenza 9 → 10 → 12 |
| T9 ∩ T10 — file `src/meshrec/core/pipeline.py` | T9 lo step 12, T10 `genera_modello` | coerente, sequenziali |
| T9, T10 → T12, T13 | `WALL_FILENAME`, `MODEL_FILENAME`, `calcola_prior`, `genera_modello` | coerente |
| T12 → T13 | `report.confronta` | coerente |
| T13 → T14 | i cinque endpoint | coerente. T14 è l'ultimo strato, non produce interfacce |
| T12 → preesistente | `quality.vertex_deviation` | esiste, `quality.py:321` |
| T2,3 ∩ tests/test_wall.py; T4,5 ∩ tests/test_abaqus.py; T7,8 ∩ tests/test_hexa.py | accodano test allo stesso file | coerente, sequenziali |

### Coerenza interna di ciascun task

| task | il suo testo concorda con sé stesso? |
|---|---|
| T1 | sì. File, produces e passi combaciano |
| T2 | sì, con una nota: dichiara `wall.fix_sign` riesportata da `abaqus.fix_sign`. Ri-esportazione senza secondo consumatore — minore, vedi deferred |
| T3 | sì |
| T4 | **no.** Il passo 8 scrive `hex_volumes` in `quality.py` e il suo test in `tests/test_quality.py`, ma i Files elencano solo `abaqus.py` e `tests/test_abaqus.py`, e i Produces non nominano `quality.hex_volumes`. Ruling A |
| T5 | sì |
| T6 | sì. Consuma `hex_volumes` da T4, coerente una volta corretto il Ruling A |
| T7 | sì |
| T8 | sì |
| T9 | sì |
| T10 | sì |
| T11 | sì. Marcato `feasibility`: `ccx` non è su questa macchina, quindi salterà |
| T12 | sì |
| T13 | sì |
| T14 | sì |
| T15 | sì. Nota di percorso: i Files dicono `meshrec/lab_telaio.yaml` mentre altri task usano percorsi relativi a `meshrec/`. Stesso file, nessuna ambiguità reale |

### Contro i Global Constraints

Nessun task chiede di scrivere in `src/` un numero del provino, di riscrivere le corse di riferimento, di reintrodurre predefiniti in `Material`, o di usare `git add -A`. I numeri del provino compaiono in T15 (`lab_telaio.yaml`) e nei test, che è dove il vincolo li ammette.

## Rulings

Ruling A: il Task 4 dichiara anche `src/meshrec/core/quality.py` e `tests/test_quality.py` fra i propri file, e `quality.hex_volumes` fra ciò che produce — il suo passo 8 li scrive già, i Files non li elencavano. Correzione del blocco Files, non del lavoro. — Perché: T6 e T7 dichiarano di consumare `quality.hex_volumes` da T4, quindi la funzione deve nascere lì; senza la correzione l'implementer del T4 potrebbe non toccare `quality.py` e T6 partirebbe su una dipendenza assente. — Costo se sbagliato: nessuno sul codice; se `hex_volumes` fosse dovuta nascere altrove, il T6 lo scoprirebbe subito e sposterebbe quattro righe.

Ruling B: nessun dispaccio di implementer in parallelo, contro la sequenza `2 || 4`, `5 || 6`, `11 || 12` proposta dal piano. — Perché: T2 e T4 modificano entrambi `abaqus.py`, e la skill vieta implementer paralleli per conflitto. — Costo se sbagliato: esecuzione più lunga in tempo di orologio, nessun costo sul risultato.

## Progresso

Ruling C: CalculiX 2.22 installato da conda-forge (micromamba, ambiente isolato in `~/.local/share/calculix-2.22`, symlink `ccx` in `~/.local/bin`), su richiesta esplicita dell'utente durante il Task 1. — Perché: il piano dava `ccx` per assente e marcava il Task 11 come saltabile; con il solutore presente quel controllo gira davvero invece di essere saltato, che è il motivo per cui esisteva. Il test di fattibilità della Fase 0 è stato rieseguito e passa su arm64, quindi il PASS della Fase 0 non è più solo di Windows. — Costo se sbagliato: nessuno sul codice; se l'ambiente sparisce, il Task 11 torna a saltare come il piano già prevede. Commit `7a42c2f`.

Ruling D: la completezza di uno sweep NON include il prior. `sweep.REQUIRED_STEPS` smette di essere un alias di `steps.STEP_KEYS` e diventa esplicitamente i soli step fino a `11_export`. — Perché: il Task 1 ha esteso `STEP_KEYS` a dodici voci e `sweep.is_complete` le pretende tutte, ma `pipeline.run` scrive fino all'11 fino al Task 9: nel mezzo ogni candidato risulterebbe incompleto e ogni fronte di Pareto uscirebbe vuoto. Ed è anche la forma giusta a regime, non una toppa di transizione: il Ruling 1 del piano tiene già `wall` e `model` fuori dall'impronta di sweep perché nessun asse della griglia li tocca e tutti stanno a monte dello step 11. Un candidato di sweep è completo quando ha il proprio deck, non quando ha il prior. — Costo se sbagliato: se un giorno uno sweep dovesse variare parametri del prior, la sua completezza andrebbe riestesa insieme all'impronta, e le due cose si toccano nello stesso punto.

Ruling E: ogni dispaccio di questa fase si apre con `preambolo-dispaccio.md` — tre invocazioni con lo strumento `Skill` (`caveman:caveman`, `ponytail:ponytail`, e almeno una skill di dominio scelta dal subagent stesso), e la risposta deve aprirsi con `Skill invocate: <elenco>`. — Perché: i primi tre dispacci descrivevano lo stile invece di ordinarne l'invocazione, e nessuno dei tre subagent ha invocato una sola skill. Segnalato dall'utente il 18/08/2026. Una descrizione è contesto, un ordine di invocazione è un'azione, e la riga di dichiarazione lo rende verificabile. — Costo se sbagliato: qualche invocazione non necessaria su task banali; il preambolo ammette l'esito «nessuna skill di dominio c'entra» purché motivato.

Task 1: fix round 1/5 (1 addressed, 0 open — completezza di sweep esclusa dal prior, Ruling D; commit fad1b83)
Task 1: fix round 2/5 (2 addressed, 0 open — due asserzioni ripristinate bit-esatte da f395ce8, commento duplicato in sweep.py deduplicato; commit ee9a3dd)
Task 1: suite verificata dal controller: 411 passati, 0 falliti, 0 saltati (da 402 passati e 3 saltati). I 3 saltati erano i test di gmsh, ora dipendenza vera.
Task 1: review — requisiti ✅, qualità approvata con riserva. 1 importante, 3 minori.
Task 1: minor (deferred): `test_una_corsa_completa_lascia_gli_undici_step_validi` verifica anche lo stato dello step 12 ma conserva il nome vecchio (tests/test_pipeline.py:215).
Task 1: minor (deferred): la chiave `"12_wall"` è letterale in tre punti (steps.py, sweep.py, test) senza un nome condiviso. Nessuna costante finché non c'è un terzo consumatore reale — ponytail.
Task 1: minor (deferred): `tests/test_pipeline.py` e `tests/test_server.py` toccati pur non essendo nell'elenco Test del brief. Ricaduta meccanica dell'estensione di STEP_KEYS, autosegnalata dall'implementer, non scope creep.
Task 1: fix round 3/5 (1 addressed, 0 open — `completa` non confronta più un numero scritto a mano ma un flag acceso dove l'ultimo step implementato finisce; commit 7de9754)
Task 1: complete (commits 7a42c2f..7de9754, review clean, 412 passati 0 falliti 0 saltati)

DA PORTARE AL TASK 9 (lo dispaccio con questo dentro): quando lo step 12 entrerà davvero in `pipeline.run()`, vanno spostate DUE cose insieme, non una — l'assegnazione di `pipeline_completa`, che oggi sta subito dopo `registra(11, ...)`, e il gate `if stop <= 11: raise _FermataRichiesta` che oggi non esiste dopo lo step 11 perché è l'ultimo. Spostarne una sola lascia una corsa fermata a 11 che si dichiara completa, oppure una corsa piena che non lo è. Segnalato dalla re-review del Task 1.

Ruling F (Task 2, sollevato dall'implementer come NEEDS_CONTEXT): il banco `TELAIO` va corretto perché ogni membratura abbia una sezione davvero diversa; l'algoritmo resta quello dichiarato dalla spec, cioè costanza dello spessore locale; e il caso degenere — telaio a sezione uniforme che collassa in una regione sola — viene reso esplicito da un test che lo dichiara, con un commento `ponytail:` che nomina il soffitto e la via d'aggiornamento (direzione locale di allungamento per cella). Scartata l'opzione di implementare subito la separazione per orientamento. — Perché: il pezzo reale ha quattro sezioni diverse (700, 250, 172, 140) ed è proprio il gradino di spessore a separare le membrature, quindi un banco a spessore uniforme non rappresenta il pezzo e lascerebbe passare un'implementazione sbagliata. Il caso degenere non produce comunque un esito falso: la regione non prismatica fallisce il controllo di costanza della sezione del Task 3 e finisce fra le scartate col proprio numero, che è esattamente il comportamento voluto dal Ruling 3 del piano. Costruire ora la separazione per orientamento sarebbe codice in più per un caso che il progetto non ha e che il sistema già dichiara invece di nascondere. — Costo se sbagliato: su una geometria a sezione uniforme il prior non trova membrature e lo dice, invece di trovarle. Se un giorno servisse quel caso, la via d'aggiornamento è scritta nel codice e il test del caso degenere diventa il test che cambia di segno.
Task 2: implementer DONE dopo il Ruling F (commits dbe197f, ab62caf). Suite verificata dal controller: 423 passati, 0 falliti, 0 saltati.

DA PORTARE AL TASK 3 (lo dispaccio con questo dentro): il Ruling F regge solo se il controllo di costanza della sezione scarta davvero una regione non prismatica — la Π che nasce quando due membrature adiacenti hanno la stessa sezione. Non è un dettaglio del Task 3, è la condizione che rende onesto il limite dichiarato nel Task 2. Il Task 3 deve avere un test che parte dal banco a sezione uniforme del Task 2, lo passa al controllo, e verifica che la regione finisca fra le scartate con il proprio numero. Se non ci riesce, il limite del Task 2 va riaperto, non aggirato.
Task 2: review — requisiti ✅, qualità approvata. 1 importante (test del caso degenere non discriminante), 2 minori.
Task 2: l'assunzione del Ruling F è stata giudicata sostenibile dal revisore: `scomponi` non conserva la mappa regione→celle, ma restituisce indici di punto disgiunti e `wall.terna` è pubblica, quindi il Task 3 può ricalcolare una terna locale per regione e misurare la costanza della sezione. Costo di ricalcolo, non perdita di informazione.
Task 2: minor (deferred): `scarta_pavimento` toglie il pavimento per appartenenza via coordinate arrotondate a 6 decimali invece che per indice, perché `segment.extract_planes` non restituisce indici. Fragile solo se un pavimento reale coincidesse a 6 decimali con un punto del pezzo.
Task 2: fix round 1/5 (2 addressed, 0 open — test discriminante sulla tolleranza verificato per mutazione dal revisore, seed indipendente per prisma; commit e290a5f)
Task 2: complete (commits 7de9754..e290a5f, review clean, 424 passati 0 falliti 0 saltati)
Task 2: minor (deferred, da chiudere nel Task 3 che tocca lo stesso file): il commento `ponytail:` in `wall.regioni()` cita il vecchio nome del test del caso degenere, rinominato in `test_una_sezione_uniforme_e_un_canarino_per_la_separazione_per_orientamento`. Puntatore morto.

Ruling G (revoca parziale del Ruling F, Task 3): il Ruling F era sbagliato nella sua parte portante. La regione a Π NON viene scartata: passa parallelismo, copertura e costanza di sezione, e viene misurata come membratura fittizia 200x1600. Dimostrato dall'implementer del Task 3 con un test, non ipotizzato. Rimedio deciso: **si aggiunge una quarta grandezza intrinseca, il riempimento della sezione** — la frazione dell'area del proprio ingombro che la sezione occupa davvero — con la soglia in `WallConfig` accanto alle altre. Scartata la separazione per orientamento locale (opzione A del Task 2), piu' costosa e non necessaria. — Perche': le misure di sezione erano tutte bounding-box, cieche a un vuoto interno; il principio 2 del prodotto dice che una soglia che non si riesce a tarare e' il sintomo di una grandezza sbagliata, e qui la grandezza mancante e' proprio quanta area la sezione occupi. Non e' una soglia cucita sul caso: un prisma ha riempimento prossimo a uno su ogni fetta, e la stessa grandezza scarta qualunque regione non piena, non solo la Π del banco. — Costo se sbagliato: una membratura legittimamente cava verrebbe scartata. E' corretto che lo sia, perche' il prior costruisce prismi pieni e modellare un tubo come pieno sarebbe un errore peggiore, ma va scritto nel documento di esito come confine dichiarato.

Task 3: due difetti nel codice del piano trovati dal TDD e corretti dall'implementer: `np.cross` su vettori 2D non piu' supportato in numpy 2.5, e assi/griglia scambiati nel calcolo del rigonfiamento con `cell_factor` fuori posto (celle troppo larghe, faccia sbagliata sorvegliata). Il piano li portava dentro il proprio codice letterale.

Ruling H (Task 3, dopo review non approvata): il riempimento resta la grandezza giusta ma la sua misura e' sbagliata e va rifatta su due punti. (1) La risoluzione della griglia trasversale deve derivare dalla spaziatura dello scanner, come ogni altra misura a celle del progetto (`cell_factor`), mai dalla lunghezza assiale divisa per un numero fisso di fette. (2) Su una nuvola di sola superficie i punti stanno solo sul perimetro, quindi «cella occupata» non puo' significare «cella che contiene punti»: significa «cella non raggiungibile dall'esterno», cioe' perimetro piu' interno racchiuso, calcolato con un riempimento a partire dal bordo dell'ingombro. Inoltre il ripiego a 1.0 quando il riempimento non e' misurabile va tolto: una grandezza non misurata non e' una grandezza pari a uno, e la regione va scartata dichiarando che il controllo non ha potuto pronunciarsi. — Perche': il revisore ha riprodotto prismi interamente pieni che scendono a 0.49 e 0.481 e verrebbero scartati come la Π, e ha mostrato che raffinando la griglia il valore peggiora invece di migliorare, firma di una misura di bordo. Il test gemello passava solo perche' il banco del Task 2 ha per caso un rapporto sezione/lunghezza favorevole. — Costo se sbagliato: se il riempimento dal bordo si rivelasse fragile su nuvole reali rade, il controllo va sospeso e dichiarato inattivo, non tarato.

Task 3: dichiarato per onesta' — la formula del riempimento e' passata da media a mediana dopo aver osservato che con la media la regione a Π dava 0.533 e sfuggiva alla soglia. Il numero 0.5 resta di principio (stessa convenzione di `face_coverage`), ma la scelta della statistica e' stata confermata guardando l'esito sul caso specifico. Va nel documento di esito della fase.

DA PORTARE AI TASK 8 E 9: `wall.Membratura` ha un quattordicesimo campo, `riempimento_sezione`, che il brief non prevedeva fra i tredici dichiarati sotto Produces. Allargamento di contratto autorizzato dal Ruling G, da onorare a valle.

Ruling I (Task 3, dopo re-review): la spaziatura con cui si misura il riempimento va stimata **sulla regione stessa**, non ereditata dal valore globale del pezzo. Si riusa `io.mean_spacing(points, sample, seed)`, che esiste ed e' gia' quella che la pipeline usa allo step 1. In piu': se la griglia locale risulta cosi' grossolana da coprire la sezione con troppo poche celle, il riempimento e' **non misurabile** — non «pieno» — e la regione finisce fra le scartate dichiarandolo. — Perche': il revisore ha dimostrato che con densita' locale quattro volte piu' rada del dichiarato un prisma pieno scende a 0.44 e viene scartato, e che con spaziatura dichiarata troppo grande una Π vera risale a 1.00 e passa come piena. E' lo stesso difetto di prima spostato da `lunghezza` a `spacing`: una risoluzione presa da una grandezza che non descrive la regione. La spaziatura locale la descrive; il guardrail sul numero minimo di celle chiude il verso opposto senza introdurre una soglia nuova, perche' si appoggia al meccanismo di non misurabilita' gia' costruito. — Costo se sbagliato: su nuvole molto rade molte regioni verranno dichiarate non misurabili e scartate. E' l'esito onesto — meglio una regione scartata con il motivo scritto che una membratura inventata — ma va detto nel documento di esito, perche' su una scansione povera il prior potrebbe non consegnare nulla.

Ruling J (Task 3, dopo la terza re-review — decisione di fondo, sostituisce l'impianto dei Ruling G/H/I sul verso dello scarto): il riempimento **smette di scartare** e diventa un esito misurato e dichiarato, a tre stati: pieno / vuoto / non verificabile. Il rifiuto si sposta da `wall.controlla` a chi costruisce, cioe' al Task 8: una membratura con riempimento basso e misura affidabile non puo' diventare un modello parametrico; una con misura non affidabile puo', ma porta con se' l'avviso dichiarato fino al report e all'interfaccia. Si aggiunge la misura di affidabilita' (dispersione delle distanze al vicino piu' prossimo, cioe' quanto la densita' e' lontana dall'essere uniforme), con il proprio limite in `WallConfig`. — Perche': tre giri hanno mostrato la stessa forma di errore spostarsi da lunghezza assiale a spaziatura globale a spaziatura di regione, e il revisore ha dimostrato che si sposterebbe ancora alla fetta e poi alla faccia, perche' una media di distanza al vicino piu' prossimo non stima una densita' bimodale a nessuna scala. La causa vera e' di ruolo: `wall.py` per spec «misura e non costruisce», e uno scarto e' una decisione di costruzione presa dentro uno strumento di misura — per scartare senza sbagliare serve una certezza che una nuvola reale non da', per misurare e dichiarare basta cio' che c'e'. Inoltre sul provino della tesi la Π non si presenta affatto (sezioni 700/250/172/140, tutte diverse), mentre il rimedio a scarto avrebbe scartato membrature buone proprio sul dato reale, scansionato da un lato solo. — Costo se sbagliato: una regione a Π raggiunge il Task 8 e deve essere rifiutata li'. Se il Task 8 dimenticasse quella guardia, si costruirebbe un modello su una membratura inventata: percio' la guardia entra nel suo brief come requisito con un proprio test, non come nota.

Nota di processo (mia): i controesempi trovati dai revisori vivevano solo nei loro messaggi, non in un file, e l'implementer del giro 4 ha dovuto ricostruirli dalla descrizione. Da qui in avanti i controesempi decisivi vanno nel ledger appena arrivano. Quelli del Task 3, come li aveva misurati il revisore:
  Caso A (scansione di scorcio): prisma pieno 250x250x1200, faccia frontale a 6 mm (8643 punti), altre tre pareti a 60 mm (249 punti). mean_spacing stimato 7,47 mm. Con lo scarto: riempimento 0,247 -> scartato pur essendo pieno.
  Caso B (tappi terminali fitti): prisma pieno 300x300x1500, due facce di testa a 8 mm (3042 punti), quattro pareti laterali a 90 mm (192 punti). mean_spacing 12,66 mm. Riempimento 0,333 -> scartato. Lo stesso prisma campionato uniformemente a 12,66 mm da' 1,0.
  Ricostruzione dell'implementer del giro 4: 300x300x1500, pareti 100 mm, facce 5 mm (rapporto 20). Piu' estrema dell'originale, stesso fenomeno; dispersione 1,42 e 1,97 -> non verificabile.

Task 3: fix round 4/5 (Ruling J attuato — riempimento a tre stati, affidabilita' da dispersione delle distanze al vicino piu' prossimo, limite `density_dispersion_limit` in WallConfig; commit 5146fd8). Suite 443 passati.
Task 3: l'implementer ha corretto tre punti del piano di fase resi falsi dal ruling (due «quattro controlli intrinseci», e lo stub `_membratura_finta` del Task 8 che non avrebbe piu' compilato). Corretto tenerli allineati.

Ruling K (Task 3, giro 5 — il tetto della procedura): l'affidabilita' del riempimento va misurata **fra le fette**, non sulla nuvola della regione. — Perche': `densita_dispersione` era un coefficiente di variazione omnidirezionale sull'intera regione, mentre il riempimento e' una mediana su venti fette lungo l'asse. Una grandezza che qualifica una misura deve descrivere la variabilita' della stessa dimensione lungo cui quella misura e' costruita. Controesempio verificato dal revisore: Π vera con meta' lunghezza a 15 mm e meta' a 150 mm esce `pieno` e `affidabile` (dispersione 0,703) perche' alcune fette rade campionano un solo montante, genuinamente pieno, e la mediana risale sopra soglia; la dispersione globale resta bassa perche' dominata per conteggio dai punti della meta' fitta, cento volte piu' numerosi. E' la stessa forma di errore dei tre giri precedenti salita di un livello: prima la grandezza sbagliata era la risoluzione, ora e' l'affidabilita' della risoluzione. — Costo se sbagliato: se anche la dispersione fra fette si rivelasse insufficiente, il tetto dei cinque giri e' raggiunto e la decisione passa a me: in quel caso il riempimento va degradato ad avviso puro, senza stato affidabile, e la guardia del Task 8 va costruita su una grandezza diversa o abbandonata dichiarandolo.

Task 3: il piano prometteva alla riga 3123 la guardia del Ruling J nel Task 8 «con un proprio test e non come nota», ma il corpo di `costruisci` mostrato nello Step 4 non la contiene e `_membratura_finta` ha `riempimento_stato="pieno"` di default: nessun test del Task 8 toccherebbe il percorso «vuoto». Correzione del piano ordinata nel giro 5.

Ruling L (Task 3, al tetto — adjudication): il Ruling K era sbagliato e l'implementer l'ha respinto correttamente. I quattro punti della sua tesi sono stati verificati in modo indipendente dal revisore, numeri riprodotti: asse della regione trasversale al taglio fitto/rado; fette che leggono 1.0 corte e non rade; un limite sui conteggi per fetta romperebbe il caso obbligatorio della Π uniforme (cv conteggi 0,696 contro cv spaziature 0,085); e il caso e' gia' fermato da `costanza_sezione` (0,535 contro soglia 0,10). La rimozione del codice per fetta e' una dimostrazione, non una comodita'. Task 3 chiuso con limite dichiarato. — Costo se sbagliato: nessuno immediato; il limite e' scritto e ha un canarino.

Task 3: fix round 5/5 (Ruling K respinto con evidenza, codice per fetta rimosso, guardia del Ruling J portata nel corpo di `costruisci` nel piano con due prove, docstring di `misura()` corretta; commit 41e1495). Suite 444 passati.
Task 3: complete (commits 501615a..41e1495 in questo giro, 23bfb11..41e1495 in tutto il task, 1 parked)

Task 3: PARKED — Ruling: su una regione a Π con densita' eterogenea `riempimento_stato` puo' risultare `pieno` con misura dichiarata affidabile pur non essendo un prisma. Oggi non raggiunge mai chi costruisce, perche' `prior` espone il riempimento solo per le membrature accettate e non per le scartate, e quella regione viene scartata da `costanza_sezione` prima. Verificato per mutazione dal revisore: allentando `section_dispersion` a 10.0 — valore che un operatore puo' scrivere in configurazione — la coppia pericolosa torna raggiungibile. Reale ma non portante: nulla a valle ci costruisce sopra, e il canarino esiste (`test_una_pi_meta_fitta_e_meta_rada_non_arriva_fra_le_membrature`). Va nel documento di esito della fase con la formula che il revisore ha scritto.

DA PORTARE AL TASK 9 (due cose, entrambe nel piano e non nel codice):
  1. `genera_modello` (piano righe 3965-3981) ricostruisce `Membratura` dal JSON di `prior` senza passare `riempimento_sezione`, `riempimento_stato` e `densita_dispersione`, che sono campi senza predefinito: seguito alla lettera solleva `TypeError` prima ancora di arrivare a `hexa.costruisci`, e la guardia del Ruling J non verrebbe mai esercitata dal percorso reale di `meshrec model`. Vanno letti da `voce["riempimento"]["stato" / "valore" / "densita_dispersione"]`.
  2. `WallConfig.section_dispersion` non dichiara nella propria docstring di essere l'unica difesa contro la coppia (pieno, affidabile) su una Π. Un operatore che la allenta in YAML non puo' saperlo. Una riga che nomini l'accoppiamento.

Task 4: implementer DONE al primo colpo, nessuna preoccupazione (commit c0e084a). Suite verificata dal controller: 452 passati.
Task 4: review — conformita' ✅, generalizzato e non duplicato, approvato con tre rilievi di robustezza. **Il vincolo piu' rigido e' verificato eseguendo**: il revisore ha generato lo stesso deck tetraedrico con il codice a 41e1495 e a c0e084a e i file sono identici byte per byte (md5 b4eefc7b834082efd9b922e3e8a20a5d, 105 righe, 30 nodi, 48 tetraedri). Verificate per esecuzione anche l'invarianza dell'ordine delle facce a permutazione degli elementi e `hex_volumes` su esaedro deformato, ruotato e rovesciato.
Task 4: fix round 1 in corso su tre rilievi — dispatch silenzioso in `boundary_faces` (qualunque conteggio di colonne diverso da 8 trattato come tetraedro), variabile locale che ombreggia la funzione pubblica `boundary_faces` dentro `export_model`, e validazione del numero di colonne che arriva dopo i calcoli geometrici e puo' far emettere un avviso su un bordo interpretato male.
Task 4: fix round 1/5 (3 addressed, 0 open — mappa esplicita dei nodi d'angolo con rifiuto dei conteggi ignoti, locale rinominata, validazione delle colonne spostata prima dei calcoli geometrici; commit e2c1d7d)
Task 4: invarianza del deck verificata end-to-end dal revisore, da 41e1495 (pre task) a e2c1d7d (post fix), con due worktree temporanei e stessa geometria: md5 e1b014f49b1557bac7628d9751e60b4e identico. Le corse di riferimento restano riproducibili.
Task 4: complete (commits 41e1495..e2c1d7d, review clean, 454 passati 0 falliti 0 saltati)
Task 4: minor (deferred): la rinomina della variabile locale in `export_model` non ha test. Un test di regressione onesto esisteva (`"boundary_faces" not in export_model.__code__.co_varnames`), ma e' una rinomina pura senza logica: assenza difendibile, non spendo un giro.

RIPRESA 20/08/2026: la sessione si era interrotta per esaurimento crediti mentre l'implementer del Task 5 componeva il messaggio finale. Stato ricostruito da git, non dalla memoria: il lavoro e' committato (111343d), albero pulito, rapporto scritto, suite 461 passati (da 454, sette test nuovi). Si e' perso solo il messaggio di chat.
Task 5: implementer DONE. Deck tetraedrico verificato identico byte per byte prima/dopo (cmp senza differenze). Non ha usato ccx: il brief lo dava facoltativo.

Ruling M (Task 5, review non approvata): il debito della Fase 1 non e' chiuso finche' la mappatura delle facce sulle etichette del solutore non ha un controllo **non circolare**, riga per riga, per entrambe le tabelle (C3D4 e C3D8). Dimostrato per mutazione dal revisore: scambiando S2 con S4 nell'esaedro la suite resta verde (461 passati); permutando l'intera tabella del tetraedro, verde ugualmente. L'unico test che oppone resistenza copre per caso solo S1 dell'esaedro, e il tetraedro — tipo predefinito di `write_inp` — non e' mai esercitato da `element_surface`/`surface_area`. `surface_area` rilegge la stessa riga che `element_surface` ha usato per scegliere: l'area torna sempre, giusta o sbagliata che sia la riga. Servono DUE controlli, con due compiti diversi: (a) un test per riga che confronti il baricentro della faccia nominata con quello atteso, calcolato indipendentemente dalla tabella — gira sempre e impedisce le permutazioni; (b) una controprova risolta con `ccx`, marcata `feasibility` — stabilisce che la convenzione trascritta sia quella vera del solutore, cosa che nessun controllo interno puo' dire, perche' se la convenzione fosse trascritta male anche l'attesa di (a) lo sarebbe. Non sono ridondanti: (a) protegge dalle regressioni, (b) rompe il cerchio. — Costo se sbagliato: (b) dipende da `ccx` e salta dove manca; se un domani sparisse, resterebbe (a), che e' comunque piu' di quanto c'e' oggi.

Ruling N (Task 5, rilievo del pass avversariale): `element_surface` deve smettere di includere facce che non sono di bordo. Oggi include una faccia se tutti i suoi nodi cadono nell'insieme dato, senza controllare l'occorrenza singola come fa `boundary_faces`: su una mesh a piu' elementi una faccia interna condivisa entrerebbe due volte e un `*TIE` o una pressione cadrebbero dentro il solido. Non emerge perche' ogni test usa un elemento isolato. Si filtra alle facce di bordo e lo si dichiara nella docstring. — Perche': tutti gli usi previsti — il `*TIE` fra membrature separate, che lega superfici esterne di corpi distinti, e i carichi laterali su facce esterne — vogliono facce di bordo. Non ci sono ancora chiamanti, quindi il cambio non rompe nulla. — Costo se sbagliato: se il Task 8 avesse bisogno di facce interne, fallirebbe in modo rumoroso e si riaprirebbe la decisione, che e' l'esito giusto.
Task 5: fix round 1/5 (2 addressed, 0 open — Ruling M e N; commit 9d89669). Il revisore ha riprodotto entrambe le mutazioni: scambio S2/S4 e permutazione completa del tetraedro ora fanno cadere il test dei baricentri (1 failed, 463 passed in entrambi i casi), mentre i vecchi test per insiemi restano verdi, il che conferma che erano loro il buco. Controprova ccx verificata non circolare: bypassando l'asserzione intermedia che usa la tabella e risolvendo con l'etichetta mutata, le asserzioni finali cadono comunque.
Task 5: deck tetraedrico invariato sull'intero task, verificato dal revisore ricostruendo il confronto da zero: 1852 byte identici da e2c1d7d a 9d89669.
Task 5: complete (commits e2c1d7d..9d89669, review clean, 464 passati 0 falliti 0 saltati; feasibility 6 passati 1 saltato)
Task 5: minor (deferred): `element_surface` reimplementa da zero il calcolo di occorrenza/ordinamento/unique gia' fatto da `boundary_faces`. Stessa logica in due posti, da tenere sincronizzata a mano. Si poteva costruire l'insieme delle facce di bordo da `boundary_faces` e testarne l'appartenenza.

DA PORTARE AL TASK 15 (documento di esito): il debito della Fase 1 e' chiuso solo a meta'. Il Task 5 ha chiuso la meta' «numerazione del solutore»: la S-etichetta scritta nel deck corrisponde davvero alla faccia fisica che nomina, verificato con `ccx` e protetto da un test per riga. Resta aperta la meta' scritta in `meshrec/docs/fase-1-debito.md:269-272`: che i set `FACE_FRONT`, `FACE_BACK`, `SIDE_LEFT`, `SIDE_RIGHT` corrispondano ai lati fisici reali del pezzo scansionato. PRODUCT.md la dichiara gia' come fatto di prodotto non deciso — «i nomi dei set di faccia sono convenzioni, non identificazioni delle facce fisiche» — ma `fase-1-debito.md` non e' stato aggiornato da nessun commit della Fase 4 e va allineato: cosa e' stato chiuso, cosa resta, e perche' la seconda meta' non e' un difetto ma un fatto dichiarato.

Task 6: implementer NEEDS_CONTEXT al primo colpo, e aveva ragione. Il test `test_lo_jacobiano_scalato_scende_su_un_elemento_schiacciato` del brief fallisce con la formula e le coordinate del brief stesso. Riprodotto dal controller in modo indipendente prima di rispondere: spostare il nodo 6 in (0.35,0.35,1.0) rende concava la faccia superiore (segni dei prodotti vettoriali fra lati consecutivi in pianta: +0.35, -0.30, +0.35, +1.00) e produce un angolo ripiegato con Jacobiano -0.4053, fuori dal range atteso.

Ruling O (Task 6, difetto del brief, non dell'implementazione): il brief asseriva una **proprieta' falsa della metrica**, e le coordinate sbagliate erano solo il sintomo. Verificato dal controller schiacciando il cubo per davvero — altezza portata da 1.0 a 0.1, geometria per il resto intatta: lo Jacobiano scalato resta **1.0 esatto**, su tutti e otto gli angoli. La metrica normalizza ogni spigolo per la propria lunghezza, quindi e' invariante di scala per direzione: misura distorsione **angolare**, non rapporto di forma. Un esaedro sottile quanto si vuole, se resta rettangolo, vale 1. Il nome del test, il suo docstring e la frase «scende avvicinandosi a zero man mano che l'elemento si schiaccia» nel docstring di `scaled_jacobian` erano tutti e tre falsi.
  Il caso degradato va ancorato a un valore noto **in forma chiusa prima di eseguire**, non a coordinate scelte finche' il numero cade nel range: un **taglio** trasforma il cubo in un parallelepipedo di spigoli (1,0,0), (0,1,0), (s,0,1), il cui Jacobiano scalato vale `1/sqrt(1+s^2)` a ogni angolo per costruzione. Verificato dal controller: s=0.5 -> 0.894427, s=1 -> 0.707107, s=2 -> 0.447214, uniforme sugli otto angoli, coincidente a precisione piena col calcolo a mano.
  Il caso concavo non si butta, si rinomina: un angolo ripiegato e' un difetto di mesh reale e distinto dall'elemento interamente rovesciato — una verifica globale di orientamento non lo vedrebbe. Diventa un test a se' con l'attesa giusta (< 0).
  — Perche': se l'implementatore avesse spostato il nodo finche' il numero cadeva in (0, 0.9) — cosa che (0.75,0.75,1.0) fa, dando 0.7542 — la suite sarebbe tornata verde documentando una bugia su cosa la metrica sa vedere. E' la quarta comparsa della verifica circolare in questa fase, stavolta introdotta dal piano invece che dall'implementazione. Si e' fermato: era la mossa giusta e va detto.
  — Costo se sbagliato: nessuno sul codice, che resta quello del brief e passa tre prove su quattro; il rischio e' solo che i tre test nuovi coprano meno del previsto, e lo si vedrebbe al Task 7, dove la mesh esaedrica vera passa sotto la stessa metrica.

Ruling P (Task 6, conseguenza del Ruling O): lo Jacobiano scalato **non misura lo schiacciamento**, quindi non e' la difesa contro gli elementi troppo sottili. Quel compito resta al vincolo di almeno tre strati nello spessore del Task 7 e alla distribuzione di `element_volume` gia' riportata da `hexa_metrics`. Non si aggiunge una metrica di rapporto di forma: la spec chiede gli Jacobiani negativi e quelli il minimo per angolo li trova. Il limite si dichiara nel docstring, dove un lettore lo cerchera'. — Costo se sbagliato: se al Task 12 il confronto avesse bisogno di distinguere due mesh con lo stesso Jacobiano ma spessori diversi, si aggiunge allora una colonna, con il caso concreto in mano invece che per ipotesi.

DA PORTARE AL TASK 7 (conseguenza del Ruling P): il vincolo dei tre strati nello spessore non e' una preferenza di infittimento, e' **l'unica difesa** contro gli esaedri troppo sottili, perche' lo Jacobiano scalato non li vede — un elemento sottile ma rettangolo vale 1 come il cubo. Va detto nel dispaccio e va scritto nel docstring di chi calcola il passo, altrimenti il primo che lo trova lo allentera' credendo di risparmiare elementi. Brief 7 riletto dal controller e sano su questo fronte: le sue asserzioni sono gia' ancorate a grandezze analitiche (volume contro il prodotto delle tre dimensioni, `scaled_jacobian > 0` come proprieta' e non come soglia calibrata, numero di piani di nodi dedotto dalla geometria), non a numeri usciti dal codice.
Task 6: implementer DONE_WITH_CONCERNS al secondo giro, dopo la correzione del brief (commit e8197fc). La preoccupazione era mia, non sua: avevo predetto 469 test e ne sono usciti 470. Aritmetica sbagliata del controller — il blocco corretto del brief ha sei funzioni di test, non cinque (quattro originali meno una sostituita piu' tre nuove). L'implementatore ha contato invece di fidarsi del numero e ha verificato la base a 464 in modo indipendente: comportamento giusto, la discrepanza andava segnalata. Suite verificata dal controller: 470 passati, 0 falliti, 1 warning preesistente non correlato. Stash vuoto e albero pulito dopo il suo `git stash` di verifica: nessuna interferenza con le altre sessioni che condividono la pila.
Task 6: review — conformita' ✅ (verificata anche fuori dal diff: `rg` su tutta la codebase non trova nessuna sottrazione ne' rapporto fra `min_ratio` e `scaled_jacobian`), qualita' buona, 0 critici e 2 avvisi. Correttezza dimostrata per mutazione: 6 mutazioni provate, 5 catturate. Il valore atteso del taglio ricavato a mano dal revisore, indipendentemente dal codice, e confermato a 0.707107. Invarianza allo schiacciamento verificata su 8 rapporti di forma e non solo su quello del test. Trappola del nodo 6 chiusa: 0.7542 riprodotto, nessun test attuale lo accetterebbe.

Ruling Q (Task 6, secondo avviso della review, difetto di nuovo mio e della stessa famiglia del Ruling O): il docstring di `hexa_metrics` afferma **al presente** che «Il confronto fra i modelli, in report.py, le tiene infatti separate e dichiara che la qualita' degli elementi non e' una grandezza confrontabile». Verificato dal controller: `report.py` non contiene una sola occorrenza di `hexa` ne' di `jacobian`, e `hexa_metrics` non ha ancora nessun chiamante — il confronto fra modelli arriva al Task 12. La frase e' falsa oggi. E' esattamente il difetto del Ruling O in un'altra forma: un'affermazione nel repository che descrive come fatto compiuto una cosa che non esiste. Si riscrive come **requisito su chi consumera' la funzione**, non come resoconto. — Perche': un docstring che mente e' peggio di uno assente, perche' viene creduto; e questo mentirebbe proprio sul vincolo che la fase esiste per proteggere. — Costo se sbagliato: nessuno, e' testo.

Ruling R (Task 6, primo avviso della review): `hexa_metrics()["inverted"]` non e' provato da nessun test — il revisore ha dimostrato per mutazione che scriverlo `0` fisso sopravvive a tutti e sette i test. Serve un caso con una mesh di **due** esaedri di cui uno solo rovesciato: con un elemento solo, `inverted == 1` sarebbe soddisfatto anche da un difetto che restituisce il numero di elementi. Il volume e' con segno (`hex_volumes` lo dichiara), quindi il totale dei due si annulla: `inverted == 1`, `hexes == 2`, `total_volume == 0`, tutti e tre noti prima di eseguire. — Costo se sbagliato: nessuno, e' un test in piu' su una funzione che al Task 12 diventera' la colonna della qualita' esaedrica nel confronto fra modelli.
Task 6: fix round 1/5 (2 addressed, 0 open — Ruling Q e R; commit 032e79e). Re-review non dispacciata: il diff e' un test piu' un paragrafo di docstring, e la verifica decisiva e' una sola mutazione, fatta dal controller di persona. Riscrivendo `"inverted": int((jacobiani <= 0.0).sum())` come `"inverted": 0` — la mutazione che il revisore aveva visto sopravvivere a tutti e sette i test — ora cade `test_le_metriche_esaedriche_contano_gli_elementi_rovesciati` con `assert 0 == 1`, e cade solo lui: 1 failed, 40 passed. File ripristinato, `git status --short` vuoto. Il docstring non nomina piu' `report.py`.
Task 6: complete (commits 9d89669..032e79e, review clean, 471 passati 0 falliti 0 saltati)
Task 6: nota di processo, vale per i task che restano. Il brief del Task 6 era sbagliato e l'ha trovato l'implementatore, non il revisore, perche' il dispaccio gli diceva di fermarsi invece di indovinare. E' la terza volta in questa fase che quella singola istruzione paga (Ruling F, K, ora O). Va in ogni dispaccio, e va accompagnata dal permesso esplicito di contraddire il piano: un implementatore che crede il brief infallibile aggiusta le coordinate finche' il numero ci sta.

Task 7: implementer DONE al primo colpo, nessuna preoccupazione (commit 719ac23). Suite verificata dal controller: 476 passati, 0 falliti. Valori attesi calcolati a mano prima di eseguire e coincidenti: volume analitico 42.000.000 mm^3, passo 140/3 = 46.6667. Il brief non ha richiesto correzioni — primo task della fase in cui il piano regge senza emendamenti.
Task 7: controllo del controller sul vincolo dei numeri del banco — `grep` su `src/meshrec/core/hexa.py` per sezioni, lunghezze, volumi e quote del provino: nessuna occorrenza. Il sorgente resta indipendente dal caso di studio.
Task 7: skill di dominio non invocata dall'implementer, con motivazione in una riga (l'unica agganciata al ruolo copre API/DB/security hardening, nessuno dei tre workflow tocca la generazione di mesh). E' l'esito raro previsto dal preambolo, non un'elusione: la riga `Skill invocate:` c'era e la spiegazione era specifica.
Task 7: review — conformita' ✅ (tutti e quattro i vincoli verificati eseguendo, non leggendo: tre strati imposti, soli esaedri anche su contorno non rettangolare, niente numeri del banco in `src/`, confine costruisci/misura intatto), qualita' buona, 0 critici, 2 avvisi, 2 note. Due mutazioni sul nucleo del brief mordono; due mutazioni strutturali sopravvivono senza che nessun test se ne accorga.

Ruling S (Task 7, primo avviso): il test dell'ordine canonico usa un cubo unitario, che e' simmetrico per permutazione degli assi — quindi non distingue la priorita' x->y->z che dichiara di verificare, e permutando le chiavi del `lexsort` sopravvive 5 volte su 5. Va rifatto su una geometria con tre estensioni **diverse** fra loro, dove ordinare per chiavi permutate produce una sequenza diversa. Il criterio di accettazione non e' una sequenza di nodi che scrivo io — sarebbe l'ennesimo numero calato dall'alto — ma un fatto verificabile: **permutando le chiavi del `lexsort` il test deve cadere.** — Costo se sbagliato: nessuno, e' un test che oggi non prova nulla.

Ruling T (Task 7, secondo avviso): un commento in `mesh_prisma` attribuisce la protezione dai prismi a base triangolare a `setRecombine(2, superficie)`, mentre il revisore ha dimostrato per mutazione mirata che a garantirla e' `Mesh.RecombineAll=1` da solo. **Terza occorrenza della stessa famiglia in due task** (Ruling O: un test che asseriva una proprieta' falsa della metrica; Ruling Q: un docstring che raccontava al presente un comportamento inesistente di `report.py`; ora un commento che attribuisce un effetto al meccanismo sbagliato). Il codice funziona in tutti e tre i casi: e' la spiegazione a essere falsa, ed e' il tipo di difetto che nessuna suite verde puo' scoprire. Va corretto attribuendo l'effetto al meccanismo vero. — Perche': un commento sbagliato sopravvive alle rifattorizzazioni e guida chi verra' dopo a togliere la riga giusta credendo di togliere quella inutile. — Costo se sbagliato: nessuno, e' testo.

Ruling U (Task 7, prima nota): `metriche["strati"]` non e' provato — un off-by-one sopravvive. Il numero di strati e' il vincolo portante di questo task, quindi il valore che lo dichiara va confrontato con i piani di nodi effettivamente presenti nella mesh, non con se stesso. — Costo se sbagliato: nessuno.

Ruling V (Task 7, seconda nota): si aggiungono le guardie sugli ingressi degeneri (`lunghezza <= 0`, contorno con estensione nulla su un asse) adesso e non al Task 8. Ponytail direbbe di aspettare il chiamante vero, ma qui il modo di fallire e' **silenzioso**: un prisma degenere non solleva nulla, produce una mesh senza volume, e il numero sbagliato arriva fino alla massa e al confronto fra modelli senza un solo segnale. In un programma che calcola strutture, un errore muto costa piu' di due righe di guardia. — Costo se sbagliato: due righe e un test in piu' su un caso che il Task 8 non produce mai; il contrario costerebbe un risultato falso creduto vero.

DA PORTARE AL TASK 8 (rilievo di design della review del Task 7, non un difetto di questo diff): `gmsh.initialize()` e `gmsh.finalize()` operano su stato **globale di modulo**, non per istanza. `mesh_prisma` e' corretto in isolamento — try/finally, nessuna perdita — ma costruisce esattamente un prisma per chiamata. L'assemblaggio del telaio ne costruisce molti: se lo facesse in parallelo, thread o processi con stato condiviso, le chiamate concorrenti si pesterebbero i piedi sullo stesso stato gmsh. Ruling anticipato: **l'assemblaggio e' seriale.** E' anche la scelta pigra, e la mesh di un telaio di poche membrature non ha bisogno di parallelismo; se un domani servisse, si isola lo stato invece di sperare che regga.
Task 7: fix round 1/5 (4 addressed, 0 open — Ruling S, T, U, V; commit f548747). Suite verificata dal controller: 479 passati, 0 falliti. L'implementatore ha confermato il Ruling T per mutazione diretta invece di adeguarsi al revisore: `Mesh.RecombineAll=1` e' davvero il meccanismo che protegge dai prismi a base triangolare, non `setRecombine` sulla superficie. Ruling S verificato contro il codice reale: il test riscritto su geometria asimmetrica cade permutando le chiavi del `lexsort` e passa con la priorita' giusta.
Task 7: la mutazione di controllo del controller e' RIMANDATA di proposito. L'architect del setaccio sta girando nello stesso worktree e potrebbe eseguire `pytest` mentre il codice e' rotto, leggendo una suite rossa che non riguarda il suo lavoro. Da eseguire a setaccio concluso, prima di dichiarare chiuso il Task 7.
Task 7: mutazione di controllo del controller eseguita a setaccio concluso. Permutando le chiavi del `lexsort` in `ordine_canonico` da `(chiave[:,2], chiave[:,1], chiave[:,0])` a `(chiave[:,0], chiave[:,1], chiave[:,2])` cade `test_l_ordine_di_nodi_ed_elementi_e_canonico_e_non_quello_dei_tag` e cade solo lui: 1 failed, 7 passed. Il test del Ruling S ora morde davvero. File ripristinato da copia fuori dal repository, `git status --short` vuoto, 8 passed.
Task 7: complete (commits 032e79e..f548747, review clean, 479 passati 0 falliti 0 saltati)

SETACCIO DEI TASK 8-15 (20/08/2026, architect, rapporto in `setaccio-task-8-15.md`): 44 rilievi, 11 BLOCCANTI, 22 SERI, 11 MINORI. Nessun task pulito. Verificati eseguendo, non leggendo: script in `/Users/mario/.claude/jobs/e87eb542/tmp/`. Il setaccio nasce da una domanda dell'utente — perche' quasi tutte le correzioni di questa fase sono state difetti del piano e non dell'implementazione — e dalla diagnosi che ne e' seguita: la prosa non viene mai eseguita, quindi non viene mai smentita.

Ruling W (Task 8, rilievo 8.1): il brief usa `np.cross` su vettori 2D, che numpy 2.0 non accetta piu'. La componente z si scrive per esteso. Non e' una svista qualunque: **la lezione e' gia' scritta nel repository a `wall.py:322`** — «np.cross rifiuta vettori 2D da numpy 2.0, quindi la componente z del prodotto vettoriale si scrive per esteso» — dentro la funzione che il brief 8 cita per nome. Ho riletto quella riga e c'e'. — Costo se sbagliato: nessuno, e' un fatto della libreria verificato dall'architect eseguendo.

Ruling X (Task 8, rilievi 8.2 e 8.3, il piu' importante del setaccio): il taglio delle giunzioni si ferma sull'**ultimo campione libero** invece che sul bordo del solido, e il vuoto che ne resta e' un artefatto di campionamento, non geometria. Misurato dal controller: 200 campioni su 1400 mm danno passo 7,0352 mm, l'ultimo libero cade a 184 x 7,0352 = 1294,4724 contro un bordo vero a 1300, vuoto 5,53 mm, errore di volume 1,63e-3. E' quel vuoto a rendere vuote le superfici del `*TIE`, cioe' a far escludere a vicenda le due cose che il Task 8 promette.
  La cura non e' allargare la tolleranza a 66,7 mm come proposto nel rapporto — curerebbe il sintomo legando nodi distanti dodici volte il vuoto, cioe' farebbe passare il test senza rendere giusto il modello, che e' la forma esatta del difetto che questa fase combatte. La cura e' **bisezione su `dentro`**, la stessa funzione di appartenenza che il taglio gia' usa, fra l'ultimo campione libero e il primo invaso. Verificato dal controller: vuoto da 5,53 mm a 3,4e-12 mm, errore di volume da 1,63e-3 a 8,8e-16. Ancoraggio non circolare disponibile e verificato: il bordo vero e' z=1300 per costruzione, e `dentro` risponde `False` a 1e-6 prima e `True` a 1e-6 dopo.
  Conseguenza: dopo la bisezione le due superfici **si toccano**, quindi la tolleranza del `*TIE` torna a essere cio' che deve essere — un margine per il rumore in virgola mobile — e non un raggio di ricerca. Va comunque passata come parametro di `dentro` e non gonfiando la geometria del prisma, perche' gonfiare il prisma lo allarga solo lungo il proprio asse e mai nella sezione. — Costo se sbagliato: la bisezione aggiunge circa cinque righe e un ciclo di 60 iterazioni per giunzione, costo trascurabile; se la sezione non fosse convessa la bisezione troverebbe *un* bordo e non necessariamente il primo, ma il soffitto dichiarato del taglio gia' esclude quel caso.

Ruling Y (Task 8, rilievo 8.2, parte sul test): la geometria dei due test condivisi e' una in cui il taglio **non avviene** — l'invasione e' una banda centrale, quindi ne' `invaso[0]` ne' `invaso[-1]`, e la lunghezza resta invariata. Il test passa perche' `min(tagliati, key=lambda p: p.lunghezza)` sceglie la trave, che non e' mai stata tagliata, e confronta 1400 con 1600. Nessuna mutazione del taglio lo uccide. Va sostituita con una geometria in cui l'intersezione tocca davvero un'estremita' del prisma minore — il caso che il soffitto dichiara di coprire — e l'asserzione va fatta **per indice**, non per `min`. — Costo se sbagliato: nessuno, il test oggi non prova nulla.

Ruling Z (Task 8, rilievo 8.6): la tolleranza del controllo di chiusura del volume e' `rel=0.15`. Con la geometria attuale l'errore vero e' 8,8% e passa. Dopo la bisezione l'errore scende a 8,8e-16, quindi la tolleranza va stretta a un valore che il campionamento non puo' piu' giustificare. Una tolleranza che copre l'errore che dovrebbe scoprire non e' una tolleranza, e' un permesso. — Costo se sbagliato: un test che fallisce su una geometria legittima, e si riapre con il caso in mano.

Ruling X — EMENDATO dall'architect, e l'emendamento e' giusto. Dicevo che dopo la bisezione la tolleranza del contatto «torna a essere un margine per il rumore in virgola mobile»: vero, ma incompleto, perche' non dicevo che **va comunque passata e non azzerata**. Misurato: a `tolleranza=0.0` le superfici del `*TIE` tornano vuote, perche' il residuo della bisezione (3,4e-12 mm) lascia i nodi appena fuori. Chi avesse letto il ruling e tolto il parametro credendolo superfluo avrebbe rifatto il difetto 8.3 in silenzio. Scelto `_TOLLERANZA_CONTATTO = 1e-6`, con provenienza dichiarabile in una riga (e' la risoluzione gia' dichiarata da `_ARROTONDAMENTO` in `hexa.py`) e la misura scritta accanto nel docstring. Quarta volta in questa fase che ha ragione l'agente dispacciato e torto io — le prime tre erano i Ruling F, K e O.

Brief 8 riscritto (`task-8-brief.md`), tutti e dodici i rilievi chiusi. Verifica dell'architect **eseguendo il brief, non leggendolo**: blocchi ```python estratti con uno script, montati con `hexa.py` in un modulo fuori dal repository, test del brief girati alla lettera — 16 su 16 passano. Poi dieci mutazioni: ogni test e' ucciso da almeno una, e ogni mutazione che un docstring nomina lo uccide davvero. Superfici del `*TIE` da 0/0 facce a 20/12; `ccx` passa da `no tied MPC: 28` a `tie constraints: 1, no tied MPC: 0`. Tolleranza del volume da `rel=0.15` a `rel=1e-9`, con l'errore vero a 1,3e-15.

DA PORTARE AL TASK 11 (lasciato aperto dall'architect, e la scelta e' giusta): il criterio che smaschera un `*TIE` muto e' `"no tied MPC" not in stdout`, che richiede `ccx`. `ccx` e' una dipendenza di `tests/feasibility/`, non della suite unitaria: metterlo in `test_hexa.py` legherebbe i test di tutti i giorni a un solutore esterno. Resta quindi fuori dal Task 8 e diventa l'estensione dovuta al Task 11 (rilievo 11.4 del setaccio, che gia' segnalava che il controllo col solutore non prova la card nuova piu' rischiosa).

Task 8: implementer DONE al primo colpo, nessuna deviazione dal brief richiesta (commit ac9fd0e). E' la conferma che il setaccio ha ripagato: il brief riparato ha retto dove la prima stesura aveva tre difetti bloccanti. Suite verificata dal controller: 487 passati, 0 falliti. L'implementatore ha rimisurato di persona i numeri piu' delicati del brief prima di scrivere codice — residuo della bisezione 4,09e-12 mm contro 3,4e-12 dichiarato, stesso ordine di grandezza; 20 e 12 facce sulle due superfici del `*TIE`, identiche.
Task 8: violazione del vincolo dei numeri del banco trovata dal controller e corretta subito (commit 3b27c3e). Il docstring di `costruisci` illustrava il disallineamento delle mesh con «una sezione da 172 contro una da 700» — sono il piedritto e la zapata del provino. Non e' logica, ma il vincolo e' che `src/` resti indipendente dal caso di studio, commenti compresi: un sorgente che nomina il provino invita chi legge a credere che il programma sia fatto per quello. Riscritto con la ragione generale, che e' anche piu' informativa: ogni membratura ha il passo della propria sezione, e due sezioni diverse danno due passi diversi. 16 test di `test_hexa.py` invariati.

Task 8: review (opus, code-reviewer + adversarial-reviewer) — conformita' CONFORME CON RISERVA, qualita' CONCERNS. Suite 487 confermata prima e dopo dieci mutazioni, albero pulito. Due requisiti del setaccio non soddisfatti (guardia del contenimento senza test; numeri del banco ancora in `src/` in quattro punti). Tre rilievi gravi, tutti e tre **riprodotti da me** prima di accettarli.
Task 8: verifica del controller sui rilievi portanti — (A2) costruita una geometria mia con sovrapposizione fuori dalla retta baricentrica (colonna 400x400 asse z, trave 100x100 asse x posta a y in [-80,20]): 200.000 mm^3 di compenetrazione vera, `taglia_giunzioni` restituisce `giunzioni: []` e la somma dei volumi resta 165.000.000, cioe' il doppio conteggio passa in silenzio. (A4) `semplifica_contorno` su un rettangolo 200x140 con una gobba di 5 mm da' `ptp = (200,140)` alla tolleranza predefinita mentre `Membratura.sezione` vale (205,140): le due grandezze divergono davvero, e la fixture `_membratura_finta` le rende identiche per costruzione. (B7) `passo_di_mesh` su 1000x10 e 90x90 con `ModelConfig()` da' 3,333 e 30,0: rapporto **nove**, non «sei volte». (B3) rifatti i due conti della mutazione: senza taglio l'eccesso e' **+2,941%**, non 8,8%; senza bisezione l'accorciamento e' **105,528** e il volume va in **difetto** dello 0,163%, non in eccesso dello 0,16%.

Ruling Y: A2 si chiude con una guardia additiva, non con un taglio piu' furbo. Il rilevamento campiona, oltre alla retta baricentrica, le quattro rette dei vertici del contorno minore; se la baricentrica non trova invasione e una delle rette dei vertici la trova, si solleva come per l'attraversamento — il taglio assiale non sa togliere una sovrapposizione d'angolo. — Perche' additiva: il caso «baricentrica dentro» continua a comportarsi esattamente come oggi, quindi nessuna geometria che oggi funziona puo' cambiare esito; l'unico esito nuovo e' un errore dove oggi c'e' silenzio. — Costo se sbagliato: una geometria d'angolo legittima (se esiste) verrebbe rifiutata invece che modellata male, ed e' il verso giusto in cui sbagliare.

Ruling Z: A1 non solleva, dichiara. Un modello con membrature scollegate e' legittimo in questo progetto — i piedistalli entrano come corpo separato per decisione dell'utente — quindi «zero `*TIE` con N>=2 membrature» non e' di per se' un errore. Va pero' reso visibile: `metriche` porta il conteggio delle membrature non legate a nessun'altra, e un `warnings.warn` lo segnala. — Perche': lo 0,1 mm di gioco che produce due blocchi in aria e la scelta di modellare i piedistalli separati generano lo stesso stato interno; solo l'operatore puo' distinguerli, e oggi non gli arriva nulla. — Costo se sbagliato: un modello slegato per errore esce comunque, con un avviso invece che con un rifiuto; il confronto fra i tre modelli lo mostrerebbe come rigidezza anomala.

Ruling AA: A3 e' un difetto di nome, non di calcolo, e va corretto in due campi distinti. `metriche["giunzioni"]` deve contare le giunzioni geometriche trovate (`len(giunzioni)`), e accanto va `metriche["ties"] = len(ties)`. — Perche': oggi il solo campo che renderebbe visibili A1 e A2 a valle e' quello che li cancella, perche' rilegge il risultato del legame invece del risultato del taglio. Con `_TOLLERANZA_CONTATTO = 0` il dizionario mostra `giunzioni=0` accanto ad `accorciamenti=[100.0]`: si contraddice da solo. — Costo se sbagliato: nessuno; sono due numeri che oggi coincidono nel caso buono e divergono solo quando qualcosa non va.

Ruling AB: A4 e' un difetto di correttezza, non di stile — il modello «primitive» va squadrato su `membratura.sezione`, non su `np.ptp(contorno)`. — Perche': `sezione` e' la misura sui punti grezzi (`wall.py:435`), `contorno` e' passato da `semplifica_contorno`, che alla tolleranza predefinita di 5 mm puo' togliere proprio il vertice estremo. Verificato: 205 contro 200 sulla stessa sezione. Il modello che la tesi presenta come «rettangolo dei valori misurati» pubblicherebbe una dimensione che non e' quella misurata. La fixture `_membratura_finta` va cambiata perche' passi una `sezione` **diversa** da `ptp(contorno)`: finche' le impone uguali, nessun test puo' vedere la differenza. — Costo se sbagliato: il rettangolo idealizzato eredita il rumore che la semplificazione toglieva, cioe' fino a `contour_tolerance` di sovrastima. E' il verso giusto: la misura si dichiara, non si liscia.

Ruling AC: i numeri falsi nei docstring dei test (B3) e la narrazione falsa sulla guardia (B4) vengono **dal brief**, non dall'implementatore — `task-8-brief.md:184-186`. E' la quinta volta in questa fase, ed e' sempre la stessa forma: un numero scritto in prosa e mai eseguito. L'implementatore ha pero' aggiunto di suo «entrambe le mutazioni verificate» a numeri che non aveva rifatto, e quello e' suo. Regola che entra nel dispaccio da qui in avanti: **un docstring che dichiara un numero di mutazione deve riportare il numero che la mutazione ha davvero prodotto in quella sessione, o non deve dichiararlo affatto.** — Costo se sbagliato: nessuno; e' piu' economico togliere una frase che verificarla.

Task 8: fix round 1/5 — implementer DONE al primo colpo (commit 1a37394), 492 passati. Ha rimisurato in proprio ogni numero che gli avevo passato come «riprodotto dal coordinatore» (A2, B1, i tre di B3, il rapporto 9,0 di B7) e coincidono tutti; ha dichiarato quali due rilievi minori non ha toccato invece di tacerli.
Task 8: verifica decisiva del controller sul giro 1 — suite riletta di persona (492), poi mutazione mia non dichiarata dall'implementatore: rimesso `np.ptp(membratura.contorno, axis=0)` al posto di `membratura.sezione` in `prisma_di`, e muore esattamente `test_il_modello_primitive_squadra_sulla_sezione_misurata_non_sul_contorno` (1 fallito / 20 passati), `assert array([200., 140.]) == approx((210.0...))`. Ripristinato, albero pulito. Rieseguita anche la mia geometria d'angolo: ora solleva il ValueError giusto.
Task 8: due residui chiusi dal controller (commit 1dea7fa, 493 passati). (1) L'avviso `MembratureNonLegateWarning` scattava anche su un modello a una membratura sola, che non ha niente a cui legarsi: diceva il vero senza dire nulla, e un avviso che si ripete su un caso normale insegna a ignorarlo. Ora tace sotto le due membrature, il conteggio in `metriche` resta. Mutazione verificata: rimettendo `if non_legate:` il test nuovo muore. (2) Il messaggio diceva «1 membrature»: riscritto come «membrature non legate: N su M».

DA PORTARE AL TASK 12: il giro 1 ha tolto da `hexa.costruisci` il campo `metriche["vincolo_giunzioni"]`, cinque righe di prosa che spiegavano perche' il `*TIE` e' una differenza fra i modelli che NON deriva dalla geometria (as-built monolitico contro parametrici vincolati alle giunzioni) e va letta accanto al confronto. Toglierlo dal dizionario dei numeri e' giusto — nessun test lo toccava e sarebbe finito duplicato — ma l'avvertenza non e' opzionale: senza, il confronto fra i tre modelli attribuisce alla geometria una differenza di rigidezza che viene dal vincolo. Deve comparire nel generatore del rapporto di confronto.

Task 8: re-review mirata del giro 1 (sonnet) — sei punti su sette CHIUSI, verificati eseguendo: A2 confrontato bit per bit col codice a 3b27c3e caricato via importlib su una geometria del revisore (accorciamento 150.00000000000063 identico prima e dopo, e la guardia sta strutturalmente dentro `if not invaso.any()`, quindi nessuna geometria che oggi funziona puo' cambiare esito); A1 su telaio a tre membrature da' `membrature_non_legate: 1`, ne' 0 ne' 3; A3 divergono con la tolleranza a zero; i cinque test nuovi muoiono tutti sotto la propria mutazione dichiarata, applicate e ripristinate una per una; i quattro numeri dei docstring rifatti in proprio e coincidono; nessun ciclo d'import e `primo_elemento` numericamente identico alla vecchia somma O(n^2) (0, 380, 968 su tre blocchi). Suite 493.

Ruling AB **emendato — avevo torto io.** Avevo scritto «tieni l'ancoraggio attuale (`minimo` del contorno) e cambia solo le due estensioni». E' la combinazione peggiore delle due: le estensioni vengono dai punti grezzi, l'ancoraggio dal contorno semplificato, e `semplifica_contorno` puo' togliere proprio il vertice che realizza il minimo. Misurato da me su una sezione 200x140 con una gobba di 5 mm sul lato x negativo (grezzo x in [-5, 200], `sezione` = 205, contorno semplificato x in [0, 200]): il rettangolo pubblicato copre x in [0, 205], cioe' **inventa 5 mm di materiale dove il dato finisce a 200 e non copre affatto i 5 mm dove il materiale c'era davvero**. Correzione: ancorare al **centro** del contorno, non al minimo — [-2,5, 202,5], errore dimezzato, simmetrico, e l'asse della membratura resta dov'e' invece di traslare. E' l'errore residuo minimo ottenibile con i dati che `Membratura` porta: il minimo grezzo non e' fra i suoi campi, solo `sezione` (estensioni) e `contorno` (semplificato). — Costo se sbagliato: il rettangolo idealizzato resta scentrato di al piu' meta' `contour_tolerance` per lato invece che di una tolleranza intera da un lato solo.
Nota: e' la **quinta** volta in questa fase che ha ragione chi esegue e non chi scrive il piano (F, K, O, X, e ora AB). Le prime quattro erano affermazioni mai eseguite; questa e' una decisione presa senza misurare la conseguenza, che e' la stessa forma con un altro nome.

Task 8: fix round 2/5 — implementer DONE (commit bad48bd), 494 passati. Ha rimisurato il difetto in proprio con `wall.semplifica_contorno` vera prima di toccare codice, invece di rileggere i numeri dal mio messaggio, e coincidono. Il test nuovo evita la trappola che avevo segnalato: contorno pulito 200x140 (minimo [0,0], centro [100,70]) con `sezione` deliberatamente diversa (210,150), cosi' minimo e centro non coincidono e l'attesa e' calcolabile su carta — [-5,205] x [-5,145].
Task 8: mutazione decisiva del controller sul giro 2 — rimesso l'ancoraggio al minimo (`centro = min + sezione/2` invece di `(min + max)/2`): muore esattamente `test_il_modello_primitive_ancora_il_rettangolo_al_centro_non_al_minimo` (1 fallito / 22 passati), `assert array([0., 0.]) == approx([-5.0 ...])`. Ripristinato, albero pulito, 23 passati nel file e 494 in tutta la suite.
Task 8: complete (commits ac9fd0e..bad48bd, re-review pulita su sei punti su sette, il settimo chiuso dal giro 2, 494 passati 0 falliti).

Task 9: implementer DONE al primo colpo (commit b6b19b0), 500 passati, nessuna deviazione dal brief riparato e nessuna delle «cose gia' verificate» risultata falsa. Ha eseguito la mutazione chiesta dallo Step 5 (sposta `pipeline_completa = True` subito dopo `registra(11, ...)`) e il test `dichiara_completa` e' morto come doveva. Riga 287 non toccata.
Task 9: mutazione decisiva del controller, diversa da quella dell'implementatore — tolto del tutto il gate `if stop <= 11: raise _FermataRichiesta`. Muoiono DUE test: `test_lo_step_dodici_si_puo_fermare_prima_con_to_step` (`'12_wall' not in metriche` cade) e `test_una_corsa_fermata_all_undici_non_si_dichiara_completa` (la chiave estranea sparisce, cioe' la corsa si e' dichiarata completa e ha sostituito invece di fondere). Il secondo e' il test che il setaccio non aveva chiesto e che ho aggiunto io al brief: senza, la meta' pericolosa della mutazione passava inosservata. Ripristinato, albero pulito, 25 passati nel file.
Task 9: costo dello step 12 **misurato** dall'implementatore su `steps.json` della corsa di prova: 0,0199 s. E' la geometria sintetica del cubo, non la scansione vera — il numero dice che sullo sweep del cubo il prior e' rumore, non che lo sia sulla nuvola reale. Da rimisurare alla prima corsa vera e da riportare nel documento del Task 15.
Nota di processo: l'implementatore ha saltato la skill di dominio dichiarandone la ragione in una riga (task di estensione pipeline+CLI su pattern esistente, nessuno dei tre workflow della sua skill calzava). E' l'esito che il preambolo ammette come raro ma lecito, ed e' stato dichiarato invece che taciuto: accettato.

Ruling AD (Task 8, riapre): **chi cede a una giunzione e' il prisma che ha l'asse invaso, non quello di sezione minore.** Trovato preparando il Task 10, misurando invece di leggere: sul telaio sintetico a quattro membrature di `tests/test_wall.py` (`TELAIO`, gia' usato dai test del prior), `hexa.costruisci` **solleva** con la guardia d'angolo del Ruling Y. Non e' una geometria patologica: e' un portale normale, ed e' la geometria piu' vicina al caso reale che il progetto abbia.

La diagnosi, per misura diretta su tutte e sei le coppie:

```
aree: [43188, 126870, 62735, 39484]
coppia (0,1): asse di 0 dentro 1:   0   asse di 1 dentro 0:   0   per area cederebbe 0
coppia (0,2): asse di 0 dentro 2:   0   asse di 2 dentro 0:  10   per area cederebbe 0   <-- SBAGLIATO
coppia (0,3): asse di 0 dentro 3:   0   asse di 3 dentro 0:   4   per area cederebbe 3
coppia (1,2): asse di 1 dentro 2:   0   asse di 2 dentro 1:   8   per area cederebbe 2
coppia (1,3): asse di 1 dentro 3:   0   asse di 3 dentro 1:   6   per area cederebbe 3
coppia (2,3): asse di 2 dentro 3:   0   asse di 3 dentro 2:   0   per area cederebbe 3
```

In ogni coppia che si sovrappone, **esattamente una** delle due direzioni ha l'asse invaso, e non e' mai ambigua. La regola per area sbaglia una volta su quattro: sulla coppia (0,2) sceglie di accorciare il traverso, ma la sovrapposizione e' il montante che entra nel traverso da sotto — accorciare il traverso lungo il proprio asse non la toglie, ed e' proprio il caso che la guardia d'angolo intercetta. La regola per asse invaso e' anche quella con un significato fisico: cede la membratura che **finisce dentro** l'altra. Una trave appoggiata su un pilastro accorcia il pilastro, non la trave.

Verificato per esecuzione, con una patch sperimentale poi rimossa: scambiando i ruoli quando l'asse del maggiore-per-area entra nel minore, il telaio costruisce — `giunzioni: 4, ties: 2, membrature_non_legate: 0` — e **tutti e 23 i test di `test_hexa.py` restano verdi**, compresi quelli del Ruling Y e delle due guardie. Albero ripristinato e pulito.

La guardia del Ruling Y **resta**: ha fatto esattamente il proprio lavoro, cioe' rendere visibile con un errore un difetto che prima contava due volte il volume in silenzio. Dopo questo cambio serve per il caso in cui **nessuno** dei due assi e' invaso e i vertici si sovrappongono, che e' una sovrapposizione d'angolo vera.

Da decidere nel giro: il pareggio. Quando entrambi gli assi sono invasi (attraversamento o contenimento) le guardie esistenti gia' sollevano; quando nessuno lo e' e non ci sono vertici invasi, si salta. Resta il caso di due assi entrambi invasi in modo parziale: li' l'area resta il criterio di spareggio, che e' deterministico.

Nota: 4 giunzioni ma solo 2 `*TIE` sul telaio sintetico — due giunzioni tagliano e poi non trovano superfici a contatto. E' il caso B2 del giro 1, e si vede **solo perche' il Ruling AA ha separato `giunzioni` da `ties`**: col campo unico di prima sarebbe rimasto invisibile. Va indagato nello stesso giro.

Task 9: review (sonnet) — conformita' CONFORME, qualita' BUONA, nessun rilievo bloccante. Verificato per mutazione, non per lettura: rimettendo `pipeline_completa = True` subito dopo `registra(11, ...)` muore `test_una_corsa_fermata_all_undici_non_si_dichiara_completa`; togliendo il gate muoiono entrambi i test dello stop. Comando `wall` provato dal vivo, non solo via test: exit 1 e stderr che nomina l'artefatto mancante **e** il comando per ottenerlo. Ha rimisurato il costo dello step 12 in proprio (0,0246 s contro 0,0199 dell'implementatore: varianza di wall-clock, nessun numero fabbricato).
Task 9: due NOTE non bloccanti, entrambe correttamente attribuite a pattern preesistenti e non a questo task — `meshrec wall` non verifica che la configurazione usata per il prior sia quella che ha prodotto `02_segmented.ply` (coerente con «la ripresa si fida dell'operatore» gia' dichiarato nella docstring di `run()`), e l'`except Exception` del ramo `wall` e' la stessa forma gia' usata in tre punti di `cli.py`. Parcheggiate: nessuna delle due e' un difetto introdotto qui.
Task 9: complete (commit b6b19b0, review pulita, 500 passati 0 falliti).

INCIDENTE DI PROCESSO (20/08/2026): il revisore del Task 9 ha usato `git stash` / `stash pop` dentro questo worktree per isolare una modifica non sua. Lo stash e' condiviso con il checkout principale e con ogni altra sessione, ed e' proprio l'operazione che le istruzioni di sessione vietano. Effetto concreto: ha **resuscitato** una patch sperimentale mia (la regola del Ruling AD) che avevo gia' scartato dopo averla misurata, lasciandola nell'albero. Nessun dato perso — l'esperimento e' registrato qui sopra e il diff e' salvato fuori dal repository in `esperimento-asse-invaso.patch` — ma il suo «500 passati» e' stato misurato con quella patch potenzialmente attiva. **Rimisurato da me su albero pulito: 500 passati**, quindi il verdetto regge lo stesso. Albero ripristinato, `git stash list` vuoto. Da qui in avanti i dispacci di revisione dicono esplicitamente: se trovi una modifica non tua, **non toccarla e segnalala**, non isolarla con lo stash.

Task 8: fix round 3/5 — implementer DONE_WITH_CONCERNS (commit 360da73), 501 passati. Ruling AD implementato in forma pulita: helper `_asse_baricentrico_invaso` con nome proprio, `invaso` riusato invece di ricampionare, docstring riscritto con la ragione nuova. Mutazione mia: rimesso il criterio per area (`if False` sullo scambio dei ruoli) e muore esattamente `test_il_telaio_a_quattro_membrature_si_costruisce_ruling_ad`, con lo stesso ValueError d'angolo. Ripristinato, albero pulito.

Task 8: la concern del giro 3 — 4 giunzioni tagliate, solo 2 `*TIE` — **indagata da me fino alla causa fisica**, e non e' quella che l'implementatore aveva ipotizzato (passo di mesh troppo grosso). Misure mie sul telaio:

```
GIUNZIONE_1 (min=2 mag=1): assi a 89.069 gradi, fuori squadra +0.931 -- nodi del maggiore dentro il minore: 1e-6mm=10  1mm=10  2mm=13
GIUNZIONE_2 (min=3 mag=1): assi a 89.524 gradi, fuori squadra +0.476 -- nodi: 1e-6mm=0  0.1mm=0  1mm=3  5mm=9
GIUNZIONE_3 (min=2 mag=0): assi a 89.240 gradi, fuori squadra +0.760 -- nodi: 1e-6mm=10  0.1mm=15  1mm=20
GIUNZIONE_4 (min=3 mag=0): assi a 89.695 gradi, fuori squadra +0.305 -- nodi: 1e-6mm=15  0.1mm=20  1mm=25
```

La causa: **le membrature vere non sono in squadra** — da 0,3 a 0,9 gradi su questo telaio — e il taglio produce una faccia **piana e perpendicolare all'asse** di chi cede, mentre la faccia di contatto dell'altro e' **inclinata**. I due piani non coincidono mai: fra loro resta un cuneo. Misurato vertice per vertice, con bisezione lungo l'asse del prisma tagliato:

```
GIUNZIONE_1: cuneo per vertice [9.93, 9.93, 0.93, 1.59, 1.59, 0.93]  max 9.930 mm
GIUNZIONE_2: cuneo per vertice [0.48, 0.66, 0.66, 0.48, 1.20, 1.20]  max 1.200 mm
GIUNZIONE_3: cuneo [1.29]  max 1.290 mm
GIUNZIONE_4: cuneo [0.42]  max 0.420 mm
```

E il cuneo spiega la soglia: la GIUNZIONE_2 ha cuneo 1,20 mm e trova i primi nodi proprio a 1,0 mm di tolleranza, zero a 0,1.

Ruling AE: **la tolleranza di contatto smette di essere una costante di modulo e diventa una grandezza per giunzione, calcolata dalla geometria** — il cuneo fra la faccia di taglio e la faccia di contatto, misurato sui vertici del contorno del prisma che ha ceduto. — Perche': `_TOLLERANZA_CONTATTO = 1e-6` e' giusto per il banco a due prismi, dove i due assi sono perfettamente ortogonali e il cuneo e' zero; su qualunque geometria **misurata** e' sbagliato, perche' il fuori squadra e' il dato, non il rumore. Il mio stesso docstring dice «non e' un raggio di ricerca e non deve diventarlo»: resta vero: il cuneo non e' un raggio di ricerca, e' la distanza che la geometria impone fra due superfici che si toccano davvero, ed e' calcolabile prima di cercarla. Degenera a zero sul banco squadrato, quindi i test esistenti non cambiano esito. — Costo se sbagliato: la tolleranza vale solo per la coppia gia' accertata dal taglio, mai come raggio globale, quindi il peggio che puo' fare e' legare due superfici di due prismi che gia' si compenetrano.

**Da non dare per scontato:** con la tolleranza giusta la GIUNZIONE_2 trova 3 nodi, e tre nodi non bastano a completare una faccia esaedrica. Non e' detto che le giunzioni legate diventino quattro. L'implementatore deve **misurare quante ne risultano**, non assumerne quattro, e se restano meno di quattro la seconda leva (il passo di mesh alla giunzione) e' una decisione separata che non entra in questo giro.

Task 8: fix round 4/5 — implementer DONE_WITH_CONCERNS (commit 9a9aff1), 502 passati. Ha contestato i miei numeri del cuneo invece di adeguarsi, **e aveva ragione**: il mio script provava entrambe le estremita' del prisma tagliato e stampava quella che dava un risultato, senza verificare quale fosse davvero l'estremita' tagliata. Sulla GIUNZIONE_1 ha misurato l'estremita' sbagliata, da cui i sei valori 0,93–9,93 mm che non corrispondono a nulla. Verificato da me: sulla faccia di taglio vera della GIUNZIONE_1 i vertici dentro il maggiore sono 0 su 6, cioe' il cuneo e' quello che dice lui. **Settima volta in questa fase che ha ragione chi esegue.** E stavolta non era prosa mai eseguita: era una misura mia, puntata sulla cosa sbagliata. Misurare e' necessario, non sufficiente — la misura va anche mirata.

Ruling AE **confermato e tenuto**, ma non fa quello che mi aspettavo. Mutazione mia: riportata la tolleranza alla costante (`tolleranza = _TOLLERANZA_CONTATTO`) e muore `test_il_cuneo_e_calcolato_dalla_geometria_e_allarga_le_facce_a_contatto` con `assert 9 == 20`. Quindi il cuneo **allarga davvero** le superfici a contatto, da 9 facce a 20 sul telaio. Ma il numero di `*TIE` resta 2 su 4: allargare la superficie non basta a completare una faccia esaedrica dove i nodi sono troppo pochi. Costo trascurabile (l'intero `test_hexa.py` gira in 2,4 s), quindi si tiene: e' piu' corretto sul dato misurato, degenera a zero sul banco squadrato, e sposta la misura nel verso giusto.

Ruling AF: **il Task 8 si chiude qui, con il limite dichiarato invece che corretto.** Su un telaio reale meta' dei giunti non produce un `*TIE`, e la causa operativa non e' piu' il cuneo (risolto) ma il numero di nodi sulla zona di contatto: la mesh del membro che fa da indipendente ha il passo della **propria** sezione, e su una zona di contatto stretta puo' non avere nodi a sufficienza per completare una faccia. — Perche' si ferma qui: le due leve che resterebbero sono entrambe scelte di prodotto, non correzioni. (a) Infittire la mesh alla giunzione, che cambia la densita' del modello e quindi il costo di ogni corsa. (b) Costruire la superficie indipendente dalle **facce** il cui baricentro cade dentro l'altro solido, invece che dai nodi tutti dentro — geometricamente difendibile per un `*TIE`, ma `abaqus.element_surface` pretende tutti i nodi di una faccia per una ragione scritta e valida sui carichi, quindi servirebbe una seconda funzione accanto, non una modifica di quella. Nessuna delle due e' una correzione di un difetto: sono due modi diversi di modellare, e la scelta cambia cosa significa il confronto fra i tre modelli. Va all'utente. — Costo se sbagliato: il modello parametrico esce con meta' dei giunti non vincolati, quindi piu' cedevole del vero, e il confronto col modello as-built attribuirebbe alla geometria una differenza che viene dal vincolo. **Per questo non e' silenzioso:** `metriche` porta `giunzioni` e `ties` separati, `GiunzioneSenzaTieWarning` lo dice a voce, e il Task 12 deve riportarlo accanto al confronto (gia' a registro come riporto).

Nota sul tetto dei giri: i giri 3 e 4 non sono tentativi falliti di chiudere un rilievo noto — sono difetti **nuovi**, trovati da me eseguendo su una geometria che nessun test copriva. Il tetto di cinque giri protegge dal rimestare, non dalla scoperta. Non l'ho fatto scattare, e chiudo per giudizio e non per esaurimento.

Task 8: complete (commits ac9fd0e..9a9aff1, quattro giri di correzione, 502 passati 0 falliti, un limite dichiarato e portato all'utente).

DECISIONE DELL'UTENTE (20/08/2026): fra le due leve del Ruling AF sceglie **(b) costruire la superficie indipendente dalle facce**, con questa motivazione: «è meglio avere una mesh omogenea piuttosto che un infittimento in aree di maggior sollecitazione». La motivazione e' un vincolo di progetto, non solo una preferenza: infittire alle giunzioni introdurrebbe una variazione di densita' proprio dove le sollecitazioni sono massime, e una mesh disomogenea li' rende meno leggibile il confronto fra i tre modelli. Il Ruling AF si riapre con la scelta fatta.

Verificato da me **prima** di dispacciare, sul telaio a quattro membrature, quante facce trova ciascuna regola sui due lati di ogni giunzione:

```
GIUNZIONE_1 (min=2 mag=1)  D: per nodi  5 -> per baricentro 12   I: per nodi  4 -> per baricentro  4
GIUNZIONE_2 (min=3 mag=1)  D: per nodi  6 -> per baricentro  9   I: per nodi  0 -> per baricentro  2
GIUNZIONE_3 (min=2 mag=0)  D: per nodi  0 -> per baricentro  5   I: per nodi  4 -> per baricentro  8
GIUNZIONE_4 (min=3 mag=0)  D: per nodi  9 -> per baricentro 21   I: per nodi 16 -> per baricentro 20
```

Le due giunzioni che restavano scollegate sono esattamente quelle con uno zero (la 2 sul lato indipendente, la 3 sul dipendente), e con la regola per baricentro **nessun lato di nessuna giunzione resta vuoto**. Quindi il telaio dovrebbe uscire con quattro `*TIE` invece di due. E' una previsione, non un fatto: va misurata dall'implementatore sul codice vero.

Nota collaterale: in questa corsa tre giunzioni su quattro hanno cuneo 0,000 mm e solo la quarta 0,429. Il Ruling AE resta giusto e la sua mutazione uccide un test (9 facce contro 20), ma il lavoro grosso lo fa la regola per baricentro, non il cuneo. Le due cose sono indipendenti e nessuna delle due rende inutile l'altra.

Task 8: fix round 5/5 — implementer DONE_WITH_CONCERNS (commit c8dc3c7), 504 passati. Ruling AF implementato: `abaqus.tie_surface` per baricentro, `element_surface` intatta. **`ties == 4` sul telaio, esattamente come avevo previsto misurando** (12/4, 9/2, 5/8, 21/20 facce per lato). Ha lasciato il test col solutore **rosso** invece di aggiustarlo, e ha verificato con un confronto A/B che il problema che ha trovato non e' una regressione del giro.

Task 8: il controllo col solutore ha trovato il difetto piu' grave della fase, e l'ho misurato io per intero. CalculiX registra tutti e quattro i `*TIE` ed esce 0, ma **61 dei 79 nodi delle superfici dipendenti non vengono legati** (77%). Il vincolo esiste sulla carta e non lega quasi nulla — ed e' esattamente il modo di fallire in silenzio che il test esiste per scoprire: nessun controllo interno lo vedrebbe, perche' dall'interno «la superficie ha facce» e' vero.

Diagnosi mia, tre esperimenti, tutti sul telaio a quattro membrature:

1. Le superfici sono **geometricamente giuste**: le dipendenti sono per intero la faccia di taglio (12, 9, 5, 21 facce), le indipendenti per intero il fianco dell'altra membratura (4, 2, 8, 20). Nessuna sovra-inclusione. La mia prima ipotesi — che il criterio per baricentro prendesse anche i fianchi del prisma tagliato — e' **falsa**.
2. Regola «una faccia entra se la tocca» al posto di «se il baricentro e' dentro», sul solo lato indipendente: le superfici crescono (4→16, 8→18, 20→35) ma la GIUNZIONE_2 resta a 2 facce, e gli avvisi scendono solo da 61 a **51**. Non e' la leva.
3. `POSITION TOLERANCE` sulla card `*TIE`, oggi assente:

```
nessuna (come oggi) -> 61 avvisi
1.0 mm              -> 44
5.0 mm              -> 32
20.0 mm             -> 32
50.0 mm             -> 32
```

**Satura a 32.** Quindi i nodi sono due popolazioni diverse: 29 recuperabili allargando la tolleranza del solutore, e 32 che non hanno alcuna faccia opposta su cui proiettarsi, per nessuna tolleranza. La causa dei 32 e' strutturale e non e' una tolleranza: due blocchi meshati **indipendentemente** hanno zone di contatto che non si corrispondono, e dove l'indipendente non ha faccia il nodo dipendente non ha nulla da legare, punto.

Ruling AG: **il Task 8 si chiude qui e la decisione va all'utente**, perche' le tre vie hanno costi e significati diversi e la scelta cambia cosa misura il confronto fra i tre modelli. (A) Accettare il vincolo parziale e **dichiararlo con un numero**: nodi legati su nodi dipendenti, in `metriche` e nel rapporto di confronto. (B) `POSITION TOLERANCE` per giunzione, dal cuneo gia' calcolato, piu' la regola «tocca» sul lato indipendente: porta gli avvisi da 61 a circa 32, non a zero. (C) **Mesh conforme fra i blocchi** — le due membrature condividono i nodi sulla zona di contatto e il `*TIE` sparisce del tutto. E' la via d'aggiornamento gia' dichiarata nel codice fin dal giro 1, e va detto che **non contraddice la preferenza dell'utente per la mesh omogenea**: conforme non vuol dire infittita, vuol dire che i due blocchi nascono da una geometria sola invece che da due. — Costo se sbagliato scegliendo (A): il modello parametrico e' molto piu' cedevole del vero e il confronto attribuisce alla geometria una differenza che viene dal vincolo. — Nulla di tutto questo e' silenzioso: il test col solutore resta **rosso** e dice perche'.

DECISIONE DELL'UTENTE (20/08/2026): sulle tre vie del Ruling AG sceglie **(B)**, dopo aver chiesto un consiglio motivato fra B e C. Il consiglio dato: B, perche' C fatta bene contraddice la scelta di mesh omogenea che l'utente stesso ha fatto poco prima — perche' due esaedri combacino nodo a nodo sull'interfaccia serve la stessa partizione dai due lati, e con passi diversi (113 mm il traverso, 60 il montante) si finisce o con un infittimento locale alla giunzione o col passo piu' fine su tutto il modello. La terza strada di C (frammentare i volumi in gmsh) rischia di perdere gli esaedri, perche' un volume frammentato spesso non e' piu' estrudibile e il ripiego sarebbe tetraedrico — e perdere gli esaedri costa piu' dei nodi non legati, visto che l'intera fase esiste per avere un modello esaedrico da confrontare col tetraedrico as-built.

Misurate le leve di B **insieme**, come promesso all'utente invece di assumerle. Telaio a quattro membrature, 79 nodi dipendenti, `ccx` vero:

```
A  baricentro, nessuna tolleranza      -> 61 avvisi 'no tied MPC'   (stato attuale)
B1 baricentro + tolleranza 5 mm        -> 32
B2 tocca + tolleranza 5 mm             -> 20
B3 come B2 ma dipendente = la piu' fitta -> 58
```

**B3 peggiora nettamente: la mia ipotesi sullo scambio dei ruoli era sbagliata**, ed e' l'ottava volta in questa fase che una supposizione mia non regge alla misura. Bene averla misurata prima di prescriverla: era esattamente cio' che avevo promesso all'utente di fare.

Tolleranza pinnata sotto la regola «tocca»:

```
nessuna -> 51     3 mm -> 20     10 mm -> 20
1 mm    -> 32     5 mm -> 20     30 mm ->  8
```

Ruling AH: **si adotta B2 con tolleranza pari allo scostamento da squadra sulla zona di contatto, non oltre.** La curva non satura — a 30 mm gli avvisi scendono a 8 — ma **quel guadagno non si prende**, ed e' una scelta, non una dimenticanza. A 30 mm la tolleranza e' dello stesso ordine del passo di mesh (46–113 mm) e della sezione: il solutore lega un nodo alla faccia indipendente piu' vicina entro 30 mm, che puo' non essere la faccia di contatto vera. Un vincolo **sbagliato** e' peggio di un vincolo mancante, perche' il mancante lo conta il nostro numero e lo stampa il solutore, mentre lo sbagliato non lo vede nessuno. Il tetto difendibile e' geometrico: `estensione della zona di contatto x sin(angolo fuori squadra)`, che su questo telaio (sezioni 200–340 mm, fuori squadra 0,3–0,9 gradi) vale 2–5 mm — cioe' esattamente dove la curva si appiattisce a 20. E' la stessa disciplina del Ruling AE: la tolleranza e' una grandezza che la geometria impone, mai un raggio di ricerca allargato finche' il numero migliora. — Costo se sbagliato: restano circa 20 nodi su 79 non legati invece di 8, cioe' si rinuncia a un quarto del recupero possibile per non correre il rischio di legami falsi. — Risultato atteso: da 61 a 20 nodi non legati su 79, cioe' dal 23% al 75% di nodi effettivamente vincolati.

Task 8: fix round 6 — implementer DONE (commit fa8340f). Suite 507 (da 504, tre test nuovi); feasibility **7 passati 1 saltato**, dove prima era 6 passati / 1 fallito / 1 saltato: il test col solutore e' tornato verde con una soglia dichiarata invece che con zero. Avvisi `no tied MPC` misurati da lui: **24** su 79 nodi dipendenti, contro i 20 che avevo misurato io con una tolleranza piatta di 5 mm — la sua e' per giunzione e derivata dalla geometria, quindi piu' prudente, ed e' il verso giusto in cui differire.
Task 8: due cose che ha fatto bene e vanno registrate. (1) Il suo primo `nodi_dipendenti_legati` era banalmente uguale al totale — se n'e' accorto da solo e l'ha rifatto con una vera distanza punto-faccia, arrivando a 55/79, che combacia con i 55 nodi che il solutore lega davvero (79 meno 24 avvisi). Una metrica che si autoconferma e' esattamente il difetto che questa fase ha pagato piu' volte, e stavolta l'ha intercettato chi la scriveva. (2) Ha dichiarato nel rapporto un proprio incidente — un ripristino da backup obsoleto che aveva cancellato l'implementazione di `POSITION TOLERANCE` insieme a una mutazione — invece di tacerlo; se n'e' accorto perche' il test falliva nel modo sbagliato.
Task 8: mutazione decisiva del controller sul giro 6 — disattivata la regola «tocca» (`if tocca:` -> `if False:`) lasciando la `POSITION TOLERANCE` attiva: il test col solutore fallisce con «32 nodi della superficie dipendente non hanno generato il vincolo», cioe' esattamente la popolazione che la regola «tocca» recupera. Ripristinato, albero pulito, feasibility di nuovo 7 passati.
Task 8: COMPLETE (commits ac9fd0e..fa8340f, sei giri di correzione, 507 passati + 7 feasibility, nessun rilievo aperto). Riepilogo del percorso: partito conforme con riserva, ha attraversato quattro difetti che nessuna revisione aveva trovato — la sovrapposizione d'angolo contata due volte in silenzio, il criterio di taglio sbagliato su un portale normale, il cuneo da fuori squadra, e il `*TIE` che il solutore accettava senza legare il 77% dei nodi. Tre su quattro sono emersi eseguendo il codice sul telaio a quattro membrature invece di leggerlo; il quarto solo perche' un solutore vero ha letto il deck.

Task 10: implementer DONE al primo colpo (commit 28cf16a), 513 passati, tutte e sette le correzioni C1–C7 applicate. Ha dichiarato di sua iniziativa che una mutazione era composta e quindi debole, invece di tacerlo.
Task 10: **due mutazioni mie, entrambe sopravvissute.** (1) Rinominata la chiave `"scostamento_nuvola"` in `pipeline.py:199`: 46 test verdi. `grep -rn scostamento_nuvola tests/` non trova nulla. La correzione C3 era bloccante proprio perche' quella grandezza «non la scrive nessuno e il Task 12 la legge»: ora c'e', e nulla impedisce che sparisca di nuovo. (2) Forzato `riempimento_stato="pieno"` nella ricostruzione della `Membratura` (`pipeline.py:153`): 46 test verdi. Era la seconda meta' di C1 — «ricostruita senza quel campo la guardia del Ruling J diventa muta» — e la guardia resta non esercitata dal percorso vero di `meshrec model`. Anche `nota_giunzioni` e i campi `nodi_dipendenti_*` non compaiono in nessun test.
Ruling AI: una correzione bloccante non e' chiusa finche' una mutazione non la uccide. Il codice giusto senza guardia e' lo stato di partenza rimandato, non un difetto risolto — e in questo caso lo dimostra la storia stessa: C3 nasce perche' quella chiave era assente e nessuno se n'era accorto per due task. — Costo se sbagliato: nessuno; sono tre test.

Task 10: fix round 1/5 — implementer DONE (commit 5c95248), 516 passati, **zero righe di produzione toccate**: il buco era di copertura, non di logica, e l'ha diagnosticato come tale invece di riscrivere codice che gia' funzionava. Ha riapplicato identiche le due mutazioni mie e ne ha aggiunta una terza sua (`nota_giunzioni` svuotata).
Task 10: riverificate da me le due mutazioni. (1) Chiave rinominata -> muore `test_lo_scostamento_dalla_nuvola_e_coerente_con_una_misura_indipendente` con `KeyError: 'scostamento_nuvola'`. (2) `riempimento_stato` forzato a "pieno" -> muore `test_la_guardia_del_ruling_j_rifiuta_una_membratura_vuota_dal_percorso_reale` con `Failed: DID NOT RAISE ValueError`. Prima erano 46 verdi in entrambi i casi. Ripristinato, albero pulito, 516.

Task 10: review (sonnet, code-reviewer + adversarial) — conformita' CONFORME su tutte e sette le correzioni C1–C7, qualita' CON RISERVE. Ha verificato eseguendo e con mutazioni **nuove**, non quelle gia' uccise nel rapporto, come richiesto. Ha eseguito il deck della corsa figlia su `ccx` vero: `Job finished`, sistema di 31.674 equazioni fattorizzato, nessun errore.
Task 10: cinque rilievi, il primo verificato da me. Raddoppiando le distanze dentro `quality.vertex_deviation` il test `test_lo_scostamento_dalla_nuvola_e_coerente_con_una_misura_indipendente` **passa lo stesso**: i due lati del confronto chiamano la stessa funzione, quindi concordano anche sbagliando. La circolarita' e' reale ed e' esattamente cio' che avevo chiesto di evitare nel dispaccio.
**Sfumatura che il rilievo non dice, e che cambia la gravita':** la stessa mutazione **uccide quattro test in `test_quality.py`** (fra cui `test_su_una_calotta_il_campionamento_dei_soli_vertici_sottostima_l_errore`, `assert 1.445 == 0.723`). Quindi la correttezza di `vertex_deviation` e' protetta, altrove. Cio' che il test nuovo protegge davvero e' il **cablaggio** — che lo scostamento venga dalla nuvola segmentata e dai nodi del modello e non da altro — ed e' una cosa utile. Il difetto e' che il **nome e il docstring promettono piu' di quello che il test fa**, e un nome che promette e' peggio di un test assente, perche' chi legge smette di cercare.
Ruling AJ: il test resta, con nome e docstring che dicono cio' che verifica davvero (il cablaggio), **piu'** un secondo controllo ancorato a una geometria di risposta nota, in cui l'RMS atteso si calcola su carta prima di eseguire. Non si risolve rinominando e basta: rinominare soltanto sarebbe declassare in silenzio un requisito che avevo posto io. — Costo se sbagliato: un test in piu' su una geometria sintetica, cioe' nulla.

Task 10: fix round 2/5 — implementer DONE (commit 700a963), 519 passati. Tutti e cinque i rilievi chiusi, ciascuno con la mutazione del revisore riapplicata e verificata. Ha dichiarato apertamente che il test rinominato **sopravvive ancora** al raddoppio di `vertex_deviation`, che e' corretto: dopo la rinomina quel test dichiara di verificare il cablaggio, e il cablaggio non cambia se l'aritmetica sbaglia. Dichiarare invece di nascondere e' il comportamento giusto.
Task 10: mutazione decisiva del controller — riapplicato `distanze * 2.0` in `quality.py:469`: muore `test_lo_scostamento_dalla_nuvola_e_esatto_su_una_nuvola_spostata_di_una_distanza_nota` con `assert 0.002 == 0.001`. E' l'ancoraggio in forma chiusa che mancava: su una nuvola traslata di una distanza nota, RMS e massimo valgono l'offset per costruzione, calcolabile su carta prima di eseguire. Il test rinominato passa, come deve. Ripristinato, albero pulito, 519.
Task 10: COMPLETE (commits 28cf16a..700a963, due giri di correzione, 519 passati + 7 feasibility, deck della corsa figlia risolto da `ccx` — Job finished, 31.674 equazioni).

Task 11: brief **riscritto e ridimensionato** dal controller prima del dispaccio. Il setaccio l'aveva analizzato il 18/08; da allora i Task 5 e 8 hanno gia' consegnato quasi tutto cio' che chiedeva — deck esaedrico C3D8I con `*SURFACE` e `*DSLOAD` (Task 5, Ruling M(b)) e telaio intero con quattro `*TIE` da `hexa.costruisci` (Task 8, giro 6). Il rilievo 11.4 («il controllo col solutore non prova la card nuova piu' rischiosa») era gia' chiuso, ed e' proprio quel controllo che ha scoperto il 77% di nodi non legati. Ridispacciare il brief originale sarebbe stato rifare due volte lo stesso lavoro: il brief nuovo porta in testa una tabella di cosa esiste gia' e da dove viene.
Task 11: corretta anche la premessa falsa che stava per finire nel documento della fase — `ccx` **e' installato** (`/Users/mario/.local/bin/ccx`, versione 2.22) e la formula «il deck esaedrico non e' stato verificato da alcun solutore su questa macchina» era falsa quanto il suo contrario.
Task 11: implementer DONE (commit 8577a6c), feasibility da 7 a **8 passati** 1 saltato, suite principale 519 invariata. La guardia sugli avvisi inattesi verificata da lui iniettando una card fittizia in `write_inp`, e **riverificata da me**: aggiunta `*CARTA_INVENTATA` al deck — che CalculiX ignorerebbe in silenzio con `returncode 0` e nessun `*ERROR` — e **falliscono tre test** con `*WARNING in calinput. Card image cannot be interpreted`. E' esattamente la classe di errore per cui la guardia esiste. Ripristinato.
Task 11: ha dichiarato una mutazione **sopravvissuta** invece di aggiustare il test — togliendo `print_nsets`, `model.dat` esiste comunque (CalculiX lo scrive vuoto) e l'asserzione di sola esistenza passava. Chiusa dal controller (commit 5f4b183): l'asserzione ora chiede gli spostamenti con `read_dat_displacements`, la stessa forma gia' usata dagli altri due test del file. Verificato: rimuovendo `print_nsets` il test ora muore con «il .dat non contiene spostamenti: il deck e' stato risolto a vuoto». Ripristinato, 8 passati.
Task 11: COMPLETE (commits 8577a6c..5f4b183, 519 passati + 8 feasibility).

Task 12: brief riparato prima del dispaccio (BASE 5f4b183). Sei correzioni D1–D6 in testa, che sovrascrivono il corpo. Verificate da me nel codice: `quality.volume_metrics` **non ha** alcuna chiave `min_ratio` — le sue chiavi sono `nodes, tets, inverted, total_volume, element_volume, min_dihedral_deg, aspect_ratio, radius_edge_ratio, radius_edge_over_reference, reference_ratio` (`quality.py:353-362`) — quindi la colonna dei tetraedri va chiamata `radius_edge_ratio`, che e' la misura, e non `min_ratio`, che in questo progetto e' il **vincolo chiesto a TetGen**. `report._testo` gestisce `None` (`report.py:293`), `_numero` no. `tests/` non e' un pacchetto.
Ruling AK: il bloccante 12.2 e' **chiuso a monte** invece che nel Task 12. Il calcolo di `scostamento_nuvola` sta nel Task 10, dove i nodi del modello e la nuvola segmentata sono entrambi a portata; il Task 12 lo legge e basta, come il suo stesso brief dichiarava («Non ricalcola nulla»), e `quality.vertex_deviation` sparisce dalle sue dipendenze. — Perche': ricalcolarlo nel confronto avrebbe significato riaprire gli artefatti della madre per ogni modello, e due punti di calcolo della stessa grandezza sono due cose da tenere allineate. — Costo se sbagliato: nessuno; se un `modello.json` vecchio non porta la chiave, la guardia D1 la rende «non impostato» invece di sollevare.
Ruling AL (nuovo, nasce dal Task 8): il rapporto di confronto **deve portare** `giunzioni` contro `ties` e `nodi_dipendenti_legati` contro `nodi_dipendenti_totali`, come numeri distinti e mai sommati o rapportati. — Perche': il Task 8 ha chiuso con un limite noto e misurato — su una geometria reale parte dei nodi dipendenti non viene vincolata dal solutore — e un parametrico con giunti parzialmente liberi e' **piu' cedevole del vero**. Senza quei numeri accanto, il confronto attribuisce alla geometria una differenza che viene dal vincolo, che e' esattamente l'errore che l'intero task esiste per evitare. Per l'as-built, che e' monolitico, quelle righe valgono «non applicabile», che non e' ne' «non generato» ne' zero. — Costo se sbagliato: il confronto diventa una tabella che confonde due cause, cioe' il difetto che rende inutile il confronto stesso.

Task 12: implementer DONE al primo colpo (commit 6c37241), 529 passati (da 519, dieci test nuovi), feasibility invariata.
Task 12: **ha trovato da solo un settimo difetto, della stessa famiglia che il task esiste per prevenire.** `confronta()` leggeva `cloud_to_mesh.rms` per lo scostamento dell'as-built, mentre i modelli parametrici lo misurano con `quality.vertex_deviation`, che e' il verso **mesh_to_cloud**. Verificato da me in `quality.py:373-425`: in `cloud_to_mesh` i campioni sono i punti della nuvola e il bersaglio e' la superficie; in `mesh_to_cloud` si campionano i **soli vertici** e il bersaglio e' la nuvola — che e' esattamente cio' che `vertex_deviation` calcola. Metterli in colonna sotto lo stesso nome sarebbe stato confrontare due misure diverse. Corretto con test di regressione che tiene i due versi a valori distinti apposta.
Task 12: mutazione decisiva del controller sul Ruling AL — collassato `"ties"` su `"giunzioni"` in `report.py:699`: muore `test_i_vincoli_alle_giunzioni_sono_quattro_numeri_distinti_e_non_applicabili_per_l_as_built` con `assert 3 == 2`. I quattro numeri sono davvero distinti e guardati. Ripristinato, albero pulito.
Task 12: minor (deferred): `CONFRONTABILI["gradi_di_liberta"]` e' dichiarato `True` ma nessuna tabella lo mostra. Ereditato dal corpo del brief, fuori dalle correzioni D1–D6, dichiarato dall'implementatore invece che taciuto. Da triagiare nella revisione finale.

Task 12: review (sonnet, code-reviewer + adversarial + ponytail-review) — conformita' CONFORME su D1–D6 tutte riverificate nel sorgente, qualita' BUONA con una riserva. Ha eseguito `meshrec compare` da riga di comando con una, due e tre cartelle e con un `modello.json` privo di `scostamento_nuvola`: tutti rc=0. Quattro rilievi.
Task 12: rilievo 1 riprodotto da me — `report.confronta([cartella_vuota])` restituiva `modelli: ['as-built']` con tutte le celle a `None`, senza eccezione ne' segnale. `confronta` distingueva la corsa madre dal parametrico **solo per assenza** di `modello.json`, cioe' su un segnale negativo, che una cartella sbagliata soddisfa allo stesso modo. Aggravante: il docstring del banco dichiarava che il confronto legge `12_wall.json`, e `report.py` non lo apriva mai — l'affermazione era falsa, e diventa vera proprio facendo la cosa giusta.
Task 12: fix round 1/5 — implementer DONE (commit 7aa4895), 533 passati. Tutti e quattro i rilievi chiusi con test RED prima del codice, mutazione applicata davvero sul sorgente e verificata, e prova da riga di comando reale per ciascuno (cartella vuota -> ValueError ed exit 1; script iniettato in `nota_giunzioni` -> escapato nell'HTML; `gradi_di_liberta` visibile in tabella; due cartelle "estruso" -> ValueError ed exit 1).
Task 12: mutazione decisiva del controller — disattivata la guardia del segno positivo: muore `test_una_cartella_senza_modello_json_ne_12_wall_json_non_diventa_un_as_built_fantasma` con `Failed: DID NOT RAISE ValueError`. Ripristinato, albero pulito, 533.
Task 12: COMPLETE (commits 6c37241..7aa4895, un giro di correzione, 533 passati + 8 feasibility). Il minore parcheggiato sul `gradi_di_liberta` e' chiuso qui, non piu' da triagiare alla revisione finale.

Task 13: brief riparato prima del dispaccio (BASE 7aa4895). Sei correzioni E1–E6, due delle quali **eseguite** e non solo lette. E1: `np.argsort(np.repeat(np.arange(len(quad)), 2), kind="stable")` e' l'identita' — `repeat([0,1],2)` da' `[0,0,1,1]`, gia' ordinato — quindi la permutazione non permuta e i due triangoli dello stesso quadrilatero finiscono agli indici 0 e **2**, non 0 e 1. Il test del corpo falliva. Sostituita con `np.stack(..., axis=1)`, verificata. E2: il gestore globale risponde `{"errore", "messaggio"}` (`server.py:274-277`), non `detail`, che e' la convenzione di `HTTPException` e qui non si usa. E5: `server.py:409-410` e' fermo a 11 in due punti scritti a mano, quindi dal Task 9 in poi lo step 12 non parte da «esegui da qui in poi». E6: `cfg_viewport` non esiste (l'idioma e' `ViewportConfig()`), e passare la cartella della corsa come cache scriverebbe dentro artefatti di sola lettura.
Ruling AM (Task 13, rilievo 13.4): `/api/rigonfiamento` **dichiara l'aggregato invece di promettere una mappa**, e non si aggiunge un artefatto. Il corpo del brief prometteva «un valore per cella» e restituiva tre numeri (min, max, p95) con un'intestazione `X-Celle` che ne dichiarava migliaia: `12_wall.json` non contiene la mappa, che vive in memoria dentro `Membratura.rigonfiamento` e non arriva su disco. — Perche' non la si scrive: nulla in questa fase la consuma, e scriverla vorrebbe dire un `.npy` accanto al JSON, cioe' un artefatto nuovo con la propria provenienza da gestire. L'endpoint restituisce i tre estremi come JSON — tre numeri non hanno bisogno del percorso binario, ed e' proprio passare di li' che rendeva credibile la promessa — e la docstring dice dove sta la mappa vera e cosa servirebbe per averla, cosi' chi la cerchera' non ricomincia da zero. — Costo se sbagliato: se il viewport vorra' una mappa di colore del rigonfiamento, servira' un task che la faccia scrivere da `wall.prior`; oggi avrebbe ricevuto tre valori credendone migliaia.
Nota di forma segnalata all'implementatore: il corpo del brief in un punto scrive un blocco sbagliato e trenta righe piu' sotto ha una «Nota vincolante» che ammette che e' sbagliato e prescrive di sostituirlo. Chi copia il blocco e prosegue non ci arriva. E3 toglie il codice sbagliato invece di lasciarlo accanto alla sua smentita, e ho chiesto di trattare allo stesso modo ogni altro punto con questa forma.

Task 14: brief riparato, **dispaccio rimandato**. Verificato che non c'e' sovrapposizione di file — il 14 tocca solo `src/meshrec/ui/` e i propri test, il 13 tocca `app/server.py` e `core/wall.py` — ma il 14 **consuma** quattro endpoint che il 13 sta scrivendo in questo momento (`/api/wall`, `/api/model/{tipo}`, `/api/compare`, `/api/membrature`), e due correzioni del 13 ne cambiano il contorno: il corpo d'errore ha `errore`/`messaggio` e non `detail`, e `/api/step/{numero}/from` passa da 11 a 12. Scrivere il lato browser contro un contratto non ancora atterrato e' il modo piu' facile per doverlo riscrivere. Si dispaccia quando il 13 rientra.
Task 14: tre correzioni F1–F3 piu' due sezioni di raccordo. F1 (BLOCCANTE) verificata da me: `illeggibile` compare **una volta sola in tutto `app.js`, dentro un commento** (riga 479) — le due funzioni del corpo solleverebbero `ReferenceError` alla prima risposta. Il sentinella vero e' `undefined` da `corpoLetto`, e l'idioma del file e' `if (corpo == null)` con `==`.
Task 14: F2 e F3 hanno **la stessa forma**, ed e' la piu' insidiosa vista in questa fase — codice che sembra funzionare e non puo', lasciato nel piano accanto alla propria smentita. Un `assert ... or True`, che vale `True` per qualunque cosa, dentro un test la cui docstring promette di sorvegliare la lezione della regione d'errore. E un `while (... && false) break;`, il cui corpo non si esegue mai mentre la `fetch` viene comunque emessa e scartata. In entrambi i casi il brief lo dichiarava sotto, in grassetto: onesto, ma inutile per chi copia il blocco e prosegue. La cura e' la stessa gia' applicata al 13: **togliere il codice inerte** e mettere al suo posto un segnaposto che non compili per sbaglio, cosi' che l'assenza fermi chi copia.

Task 13: implementer DONE_WITH_CONCERNS (commit e534046), 543 passati (da 533, dieci nuovi), feasibility invariata.
Task 13: **ha trovato per esecuzione due difetti che le mie sei correzioni non coprivano.** Il primo e' serio e nasce proprio da E3: `wall.scarta_pavimento` non restituiva la maschera dei punti tenuti, quindi gli `"indici"` che E3 chiedeva di aggiungere a `wall.prior` erano indici della nuvola **ripulita** usati contro la nuvola **intera** — ogni volta che il pavimento veniva tolto, le etichette delle membrature cadevano nel pavimento invece che sulla membratura. Riprodotto con una nuvola sintetica col pavimento prima del telaio, corretto aggiungendo la maschera come terzo valore di ritorno. Il secondo: il test di `/api/compare` del corpo non poteva passare come scritto, perche' `report.confronta` rifiuta una corsa madre senza `12_wall.json` — che e' la guardia del segno positivo aggiunta dal Task 12 poche ore prima. Ha corretto il test invece di indebolire la guardia, che e' il verso giusto.
Task 13: mutazione decisiva del controller — tolta la rimappatura (`indici_pieni[m.punti]` -> `m.punti`): muore `test_le_membrature_etichettano_i_punti_anche_quando_il_pavimento_e_stato_tolto` con «gli indici della membratura cadono nel pavimento: lo sfasamento...». Ripristinato, albero pulito, 543.
Task 13: minor (deferred): `/api/membrature` etichetta i punti con un ciclo Python su `gruppi`, fino a 400.000 iterazioni. Rapido sui banchi sintetici, **non misurato** sulla corsa reale. Marcato nel codice con un commento `ponytail:` e un'alternativa vettorizzata. Da rivalutare quando il Task 14 lo usera' davvero dal browser — e' il primo consumatore vero.

Task 14: implementer DONE_WITH_CONCERNS (commit a00c91a), 555 passati (da 543, dodici nuovi), feasibility invariata.
Task 14: ha trovato un **terzo difetto della stessa famiglia** di F2/F3, non coperto dalle correzioni: il codice dei due bottoni non passava lo scanner preesistente sulla disciplina dell'ordine, e dietro c'era una race vera sul viewport condiviso. Corretta catturando `ordine` prima dell'attesa. Ha anche trovato **guardando nel browser, non nei test**, che `caricaConfronto` non controllava `risposta.ok` e sollevava un `TypeError` in console alla primissima apertura della pagina, cioe' prima che esista una corsa madre — il caso in cui si trova ogni utente nuovo. Corretto con test di regressione.
Task 14: ha colmato un vuoto del brief — `GET /api/membrature` non era chiamato da nessuna parte nel corpo, quindi l'endpoint del Task 13 non aveva consumatori. Aggiunto riusando l'arbitro `apriGeometria`/`ultimaGeometria` gia' esistente invece di inventarne un altro.
Ruling AN: **la corsa end-to-end sulla scansione di riferimento non viene eseguita in questa fase, e il documento lo dichiara.** Verificato: `meshrec/lab.yaml` punta a `../Nuvole di punti/lab_frame.pcd`, percorso **relativo** che dal worktree isolato non risolve; il file vero e' in `/Users/mario/GitHub/Tesi/Nuvole di punti/lab_frame.pcd` e pesa 152 MB. — Perche' non si aggira: copiarlo nel worktree o lanciare la corsa sono ore di calcolo su una spesa che decide l'utente, e farlo di iniziativa propria sarebbe prendere una decisione sua. — Cosa fa il Task 15 invece: scrive tutto cio' che e' stato misurato davvero (la suite, i controlli col solutore, i banchi sintetici, il telaio a quattro membrature), **dichiara** che la corsa vera non e' stata eseguita e perche', riporta il comando esatto col percorso assoluto, e **non scrive alcun numero** che sarebbe potuto uscire solo da quella corsa. — Costo se sbagliato: il documento della fase descrive un programma verificato su geometrie sintetiche e su un solutore vero, ma non su **la** geometria della tesi; e' un limite reale e va letto come tale, non nascosto.
Task 14: minor (deferred), riportato ma **non chiuso**: il ciclo Python di `/api/membrature` resta non misurato sulla nuvola vera, per lo stesso motivo del Ruling AN. Il primo consumatore reale c'e' ora, la misura no.

CORSA VERA ESEGUITA (21/08/2026, autorizzata dall'utente). Ruling AN **revocato**: la corsa e' stata fatta. Configurazione dell'utente (C25/30, E 31500, ν 0.2, ρ 2.5e-9), nuvola `/Users/mario/GitHub/Tesi/Nuvole di punti/lab_frame.pcd` (152 MB), uscita in `runs/lab_telaio/` (gitignorata, nessuna cartella di sola lettura toccata). `lab.yaml` **non toccato**, per non collidere col Task 15 in corso: usata una copia con percorso assoluto.

Esito: **dodici step su dodici riusciti, in circa 80 secondi in totale** — non le ore stimate. Lo step piu' lento e' il 12 (prior) con 48,95 s; il secondo e' il 02_segment con 17,88 s. Tutti gli altri sotto i 5 s.

Modello as-built prodotto e valido: 13.237 nodi, 50.728 tetraedri, **0 invertiti**, volume 168.845.534 mm³ (0,1688 m³), errore geometrico mesh_to_cloud RMS 4,356 mm su 9.659 campioni, massimo 72,24 mm. Deck `wall_model.inp` da 2,5 MB.

**IL RISULTATO CHE CONTA — il prior non decompone il telaio reale: tre regioni trovate, ZERO membrature accettate.**

```
punti_per_regione: [4212659, 3567, 1455]     spessore_mediano: 192,02 mm
regione 0 (4.212.659 punti): costanza_sezione 1,1830 contro soglia 0,1   -> RESPINTA
regione 1 (3.567 punti):     parallelismo 32,32 gradi contro soglia 5    -> RESPINTA
regione 2 (1.455 punti):     parallelismo 31,95 gradi contro soglia 5    -> RESPINTA
```

Il 99,9% dei punti finisce in **una sola** regione. E la causa e' scritta nel codice da prima che la corsa esistesse: `regioni()` raggruppa le celle per **spessore** — due celle adiacenti sono la stessa membratura se gli spessori differiscono meno di `thickness_tolerance` — e il commento `ponytail:` a `wall.py:194-203` dichiara il soffitto per esteso: «due membrature adiacenti con la stessa sezione non hanno alcuna discontinuita' da cui tagliare e restano una regione sola». Il telaio del laboratorio ha membrature di spessore simile (mediana 192 mm): sta esattamente sopra quel soffitto.

**Il sistema non ha mentito.** Non ha inventato membrature: ha misurato `costanza_sezione = 1,18` contro una soglia di 0,1 e ha respinto. E `meshrec model` si rifiuta di costruire, con il messaggio giusto: «nessuna membratura da costruire: il prior non ne ha accettata alcuna. Guarda le regioni scartate e il controllo che le ha respinte, invece di generare un modello vuoto». La catena di guardie costruita in questa fase ha funzionato: il fallimento e' arrivato **dichiarato**, non in silenzio.

Conseguenza per la fase: sulla geometria della tesi i **modelli parametrici non sono generabili oggi**. Il confronto a tre modelli ha una sola colonna, l'as-built. La via d'aggiornamento e' gia' nominata nello stesso commento: «direzione locale di allungamento per cella (PCA sull'intorno) invece del solo spessore».

Da verificare con l'utente, non da concludere qui: il volume misurato (0,1688 m³) e' circa il 35% del `volume_atteso` di 0,4777 m³ che il brief del Task 15 porta per `lab.yaml`. Puo' essere il ritaglio che copre solo parte del telaio, o altro. Non ho verificato da dove viene quel numero, quindi lo segnalo come da controllare e non come scarto.

Task 15: implementer DONE_WITH_CONCERNS (commit 85e5d1e), suite invariata (555 + 8 feasibility). **Ha trovato due errori miei, entrambi confermati da me.**

(1) **Un numero della mia correzione G4 era scaduto.** Avevo scritto «31.674 equazioni» come misurato; lui ha riprodotto lo scenario e ha ottenuto **32.637**, scrivendo nel documento il numero suo con la fonte e dicendo esplicitamente che il mio non corrispondeva. Riverificato da me: `number of equations -> 32637`. Gli altri numeri di G4 reggono tutti (ties 4, nodi dipendenti 79, non legati 24). L'origine dell'errore: i 31.674 li aveva misurati il revisore del Task 12 **prima** che i giri 3-6 del Task 8 cambiassero il criterio di taglio e le superfici del `*TIE`, che cambiano la mesh. Era vero allora, falso adesso, e io l'ho riportato senza rimisurarlo. **Nona volta in questa fase**, e di una specie nuova: non un'affermazione mai eseguita, ma **una misura scaduta**. La regola «ogni numero o l'hai misurato o non lo scrivi» va letta col tempo dentro: misurato *quando*, e su quale codice.

(2) **La mia corsa vera ha usato il ritaglio sbagliato.** Avevo copiato il ritaglio da `lab.yaml` (largo 290 mm in y, z fino a -480). Il Task 15 ha invece **misurato il ritaglio dalla nuvola**: piano del pavimento trovato con RANSAC (normale 0.005, -0.006, 1.000, inlier fra z=-522,5 e -498,5), e in y la fascia dove l'ampiezza crolla da 441 a 217 mm, cioe' la transizione zapata->colonna. Ne esce `lab_telaio.yaml` con crop largo **750 mm** e z fino a **-498**, che comprende le zapatas — i piedistalli che l'utente aveva detto essere parte del modello. Il ritaglio di `lab.yaml` e' «il solo telaio» e taglia via proprio i corpi che la Fase 4 doveva includere. Corsa rifatta con la configurazione misurata, in `runs/lab_telaio_v2/`.

Nota di merito: il Task 15 non ha lanciato corse concorrenti ne' toccato le cartelle della mia, e ha segnalato la discrepanza invece di adeguarsi al mio numero. E' il comportamento che tutta la fase ha cercato di ottenere.

CORSA VERA v2 (ritaglio misurato, `runs/lab_telaio_v2/`): dodici step su dodici riusciti. **Il ritaglio non era la causa.** Con il crop misurato dal Task 15 (750 mm in y, z fino a -498, zapatas comprese) le regioni passano da 3 a **8**, ma le membrature accettate restano **zero**, e una regione tiene ancora il 99,3% dei punti.

```
reg  0  punti 4.215.879  parall  2,29  copert 1,00  costanza 1,187  -> costanza_sezione
reg  1  punti    14.811  parall 10,29  copert 1,00  costanza 0,572  -> parallelismo, costanza_sezione
reg  2  punti     8.059  parall 10,49  copert 1,00  costanza 0,782  -> parallelismo, costanza_sezione
reg  3  punti     3.772  parall  5,07  copert 1,00  costanza 0,608  -> parallelismo, costanza_sezione
reg  4  punti     3.267  parall 31,95  copert 1,00  costanza 0,241  -> parallelismo, costanza_sezione
reg  5  punti     1.351  parall 36,14  copert 1,00  costanza 0,197  -> parallelismo, costanza_sezione
reg  6  punti     2.513  parall  1,11  copert 1,00  costanza 0,572  -> costanza_sezione
reg  7  punti       380  parall  6,17  copert 1,00  costanza 0,173  -> parallelismo, costanza_sezione
```

**Tutte e otto falliscono `costanza_sezione`, senza eccezione.** La regione 0 lo fallisce a 1,187 contro 0,1 mentre il suo parallelismo e' ottimo (2,29 gradi contro 5): non e' una regione mal misurata, e' **il telaio intero preso per un prisma solo**. Le altre sette sono briciole.

Con i riscontri dichiarati dal Task 15 il prior **dichiara da solo il proprio fallimento in numeri**: `membrature_attese: 6`, `scarto_membrature: -6`, `volume_atteso: 477.700.000 mm³`, `scarto_volume: -1.0`. Nessuna interpretazione richiesta.

Diagnosi definitiva, e coincide con il soffitto scritto in `wall.py:194-203` prima che la corsa esistesse: `regioni()` raggruppa le celle per **spessore**, e membrature adiacenti con la stessa sezione non hanno alcuna discontinuita' da cui tagliare. Lo spessore mediano del telaio e' 192 mm su tutte le membrature. La via d'aggiornamento e' nominata nello stesso commento: **direzione locale di allungamento per cella (PCA sull'intorno) invece del solo spessore.**

Il Ruling AN e' revocato due volte: la corsa e' stata fatta, e poi rifatta col ritaglio giusto. Il documento del Task 15 va aggiornato con questi numeri, che adesso esistono.

VIA DELLA PCA — prototipata sulla nuvola vera (`runs/lab_telaio_v2/02_segmented.ply`, 4.269.608 punti, 68.089 celle), **non implementata**. Riporto le misure perche' valgono piu' della conclusione.

Formulazione 1 — PCA sui centri delle celle occupate, in un disco di raggio R celle:
```
R=6   anisotropia mediana 0,000   regioni 2297 (identiche a oggi)   accettate 0
R=30  anisotropia mediana 0,270   regioni 2297 (identiche a oggi)   accettate 0
```
La prima versione esentava dal taglio gli archi con una cella isotropa — cioe'
esentava **esattamente le giunzioni**, che sono il posto da tagliare. Invertita
la regola (l'arco regge solo se entrambe le celle hanno direzione affidabile e
le due coincidono): regioni 49.929, e **da 0 a 3 membrature accettate**. Ma le
sezioni escono `[224,3 x 47,8]`, `[216,3 x 46,1]`, `[230,9 x 48,2]`: quel 47 mm
su membrature nominali da 172-250 mm dice che non sono membrature, sono **lamine
di superficie**.

La ragione, ed e' il punto: la PCA sull'occupazione misura **quanto una cella e'
vicina al bordo della sagoma**, non lungo quale asse corre la membratura.
Nell'interno di una striscia larga l'intorno e' un disco pieno, quindi isotropo;
solo sul bordo diventa allungato. Tenere gli archi fra celle anisotrope tiene
percio' le sole bucce.

Formulazione 2 — PCA sui punti veri in un intorno, invece che sui centri di
cella: `anisotropia mediana 0,087`, regioni 2297, **accettate 0**. Stesso difetto
a scala diversa. (Da segnalare per chi riprendera': `query_ball_point` a 250 mm
su 300.000 punti satura la memoria — servono i k vicini con un tetto di raggio.)

Ruling AO: **la via della PCA sull'occupazione non e' la strada, e non la si
implementa.** Per una sagoma **piena** la PCA locale e' isotropa nell'interno a
qualunque scala, finche' il raggio non supera la larghezza della striscia — e a
quel raggio la zona di giunzione prende dentro entrambe le strisce. Le due
formulazioni misurate falliscono per la stessa ragione, da due lati opposti. —
Cosa serve davvero: **l'asse mediale (scheletro) della sagoma**, i cui rami sono
gli assi delle membrature e i cui punti di diramazione sono le giunzioni.
Trasformata di distanza piu' assottigliamento: e' ben posto, ma e' un pezzo di
lavoro con un proprio progetto, non un innesto da fine sessione. — Costo se
sbagliato: nessuno, perche' non ho scritto codice; se qualcuno trovasse una
formulazione della PCA che regge, queste misure gli dicono contro cosa
confrontarla. — **Non dispaccio un task su un piano che non sono riuscito a far
funzionare in prototipo**: sarebbe la decima volta della stessa famiglia, con la
differenza che stavolta lo saprei in anticipo.

Task 15: **terzo errore mio trovato dallo stesso implementatore, in un solo giorno.** Gli avevo passato i numeri dell'as-built come misurati sulla corsa v2; erano quelli della **v1**, la corsa col ritaglio sbagliato. Ha confrontato con `runs/lab_telaio_v2/metrics.json` invece di fidarsi, e ha scritto nel documento i veri. Riverificato da me:

```
runs/lab_telaio      nodi 13237  tet 50728  inv 0  vol 168.845.535  RMS  4,355
runs/lab_telaio_v2   nodi 14103  tet 51913  inv 0  vol 217.728.361  RMS 27,538
```

**Decima volta in questa fase**, e la terza variante della stessa forma: non un'affermazione mai eseguita (le prime otto), non una misura scaduta (la nona), ma **una misura letta dalla cartella sbagliata**. La regola completa e' quindi: ogni numero o l'hai misurato **tu**, **adesso**, **sulla cosa di cui stai parlando**, o non lo scrivi. Le tre varianti rompono tre pezzi diversi di quella frase.

Da portare all'utente, perche' e' un dato e non un dettaglio: sul ritaglio misurato (zapatas comprese) l'errore geometrico dell'as-built e' **27,54 mm di RMS** contro i 4,36 del ritaglio stretto. Il volume passa da 0,1688 a 0,2177 m³ contro un atteso di 0,4777. La superficie di Poisson ricostruisce molto peggio la geometria completa di quanto facesse sul solo telaio.

Task 15: COMPLETE (commits 85e5d1e, 42ba116). Ha verificato ogni numero del mio messaggio prima di scriverlo, e ne ha corretti tre.

REVISIONE FINALE DEL RAMO (opus): conformita' ai vincoli di prodotto **conforme**, qualita' del ramo **alta**, **nessun bloccante**. Ventuno rilievi di rifinitura. Due riprodotti da me: la cartella figlia orfana (`runs/lab_telaio-estruso/` con dentro il solo `config.yaml`, creata dal mio tentativo fallito) e la tabella `FACCE_DEL_SOLUTORE` senza test sull'ordine — mutata da perimetro a farfalla, **555 verdi**.
ONDA DI CORREZIONE: sedici su sedici chiuse, sette commit (49e9c4c..09c33aa), **563 passati** (otto test nuovi) e feasibility invariata. L'implementatore ha **ridotto F7 di sua iniziativa** dopo aver verificato che due dei tre punti citati parlano di un'altra via d'aggiornamento: il revisore gli ha dato ragione per intero. Rifiutare di correggere e' legittimo quanto correggere, quando il motivo regge alla lettura.
Mutazione decisiva del controller sull'onda: riapplicata la farfalla, muore `test_facce_del_solutore_c3d8_sono_cicli_di_perimetro_non_diagonali` con «(4, 7, 5, 6): (5, 7) non e' uno spigolo, e' una diagonale». Il test itera su tutte e sei le facce, non solo su quella mutata. Ripristinato, 563.
RE-REVIEW MIRATA: sedici CHIUSE, **nessuna rottura nuova**, entrambe le suite verdi, albero pulito, i tre parcheggiati confermati non toccati.

FASE 4 CHIUSA. Quindici task, cinquantasei commit, 563 test piu' 8 di feasibility. Documento in `meshrec/docs/fase-4-prior-telaio.md` (537 righe, i rulings alla sezione 5).
